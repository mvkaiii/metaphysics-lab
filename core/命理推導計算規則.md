# 命理推導計算規則

- 文件版本：v1.4（規則文件版本；目前軟體正式 release 為 v1.5.0）
- 八字流時引擎：Metaphysics Lab 八字時間推導引擎 v1.0.0
- 八字 Natal：`engine/bazi/natal.py` / `bazi.natal_chart` = implemented / experimental / on_demand / 1.0-exp
- 紫微 Natal：`engine/bazi/natal.py` / `ziwei.natal_chart` = implemented / experimental / on_demand / 1.0-exp
- Natal reconciliation：`engine/natal/reconciliation.py` = implemented / stable / on_demand / 1.0
- Natal Markdown export：`engine/natal/export.py` = implemented / stable / on_demand / 1.0
- 紫微 Fine Cycle：`engine/ziwei/fine_cycle_stems.py`、`engine/ziwei/fine_cycle.py`
- 中立干支 helper：`engine/calendar/sexagenary.py`
- Historical Activation Selector：`engine/historical/selector.py` / `historical.activation_selector` = implemented / experimental / on_demand / 1.0-exp

## 文件定位

本文件規範 Metaphysics Lab 的 deterministic calculation boundary，包含：

1. 由出生資料建立 **Project 原生盤面** 的 Natal Chart Foundation。
2. 由本命／運限／目標時間建立 **Project 推導盤面** 的時間算法。
3. Calendar、Bazi、Ziwei 各自的時間責任邊界。
4. Historical Blind Calibration 所使用的 deterministic historical activation selection。

第三方來源直接提供的資料仍是原始盤面事實；Project 原生盤面與 Project 推導盤面不得冒充 Astralium、iztro、原始 PDF 或其他第三方直接輸出。

---

# 一、Phase 2C0 Natal Chart Foundation

## 1.1 輸入契約

完整出生資料 Mode A 最低輸入：

- sex
- Gregorian birth date
- birth time
- birth place

Location Resolver 將 birthplace name 解析為 label、latitude、longitude、IANA timezone 與 provenance；Calendar Resolver 再接受 structured local civil datetime + timezone。

固定原則：

> **Precision must be earned by input.**

輸入不足或不唯一時，只能追問、保留候選或降級分析，不得自行補值。

已知四柱 Mode B 只能建立 Bazi imported/external natal view；不得用四柱反推唯一出生日期／時間，也不得重建完整 Ziwei natal。

Astralium 或其他結構化第三方資料 Mode C 視為 external view；Project 仍可依出生資料獨立重算，兩者進 reconciliation，不互相覆寫。

## 1.2 時間 views

Shared civil base 必須保留，不做全域真太陽時覆寫。至少保留：

```text
reported_civil_time
normalized_civil_time
bazi_effective_time
ziwei_effective_time
```

必要時另保存 true-solar calculation record、adjustment minutes、profile_id、rule_version、calculation_basis、provenance。

**Calendar Resolver 不負責真太陽時。** 它維持 neutral civil/calendar infrastructure；civil date 於 00:00 換日，23:00 已屬子時但不由 Resolver 套用命理 effective-day policy。

## 1.3 真太陽時

Phase 2C0 的真太陽時計算口徑固定記錄為：

> **真太陽時＝經度校正＋均時差**

概念上：

```text
true_solar_adjustment = longitude_correction + equation_of_time
true_solar_time = normalized_civil_time + true_solar_adjustment
```

此公式由 `engine/birth/time_views.py` 的 versioned profile 實作與測試；不得在文字層自由重算。

八字與紫微 time profile **獨立**：

- Bazi Natal v1 採自己的 Project profile，civil effective time 與 true-solar candidate 分開比較；若兩者四柱相同可視為 EQUIVALENT，若跨日柱／時柱等實質邊界則標記 material conflict，不得靜默選邊。
- Ziwei Natal v1 default profile 為 `ziwei-natal-true-solar-common-v1`，會由 Project 自動套用上述真太陽時校正；普通使用者不需自行選「要不要真太陽時」。原始 civil time 永遠保留。

真太陽時差異只有在造成實質盤面差異時才需要突出顯示；未跨時辰／日期等核心邊界時，普通 UX 可只顯示已校正且未改盤。

## 1.4 Project Bazi Natal

正式 capability：

```text
bazi.natal_chart = implemented / experimental / on_demand / 1.0-exp
```

Project Bazi Natal v1 deterministic facts 包含：

- 四柱年／月／日／時
- 日主
- 藏干
- 十神
- 可重現的基礎五行資料
- 大運方向、起運資料與大運干支
- time basis / profile / provenance / validation

格局判斷、喜用神結論、主觀強弱分數、神煞大全不因 Natal Builder 存在就自動變成盤面事實。

## 1.5 Project Ziwei Natal

正式 capability：

```text
ziwei.natal_chart = implemented / experimental / on_demand / 1.0-exp
```

Project Ziwei Natal v1 deterministic core 包含：

- 採用的 birth/calendar/time basis
- 農曆年／月／日／時辰資料
- 命宮、身宮、十二宮與宮干
- 五行局
- 已正式納入 catalog 的主星／輔星位置
- 來源有定義時的亮度資料；未定義不得猜值
- 生年四化及既有 Phase 2A flying integration
- 大限方向／宮位／年齡區間
- profile / rule version / provenance / validation

Natal Builder 與 `ziwei.flowing_stars` 是獨立 capability；不得因其中一項存在就推論另一項 maturity，當前狀態以 `runtime_info` 為準。

## 1.6 External / Project / Resolved

本命 cross-system model 固定三層：

```text
External / Project / Resolved
```

reconciliation status：

```text
MATCH / EQUIVALENT / CONFLICT / NOT_COMPARABLE
```

severity：

```text
INFO / CAUTION / BLOCKING
```

External / Project raw view 不得覆寫。Experimental authority 若與 external 衝突，Resolved 預設採 external；未來 capability 經正式 promotion 為 Stable 後可改採 Project，但 CONFLICT 本身不能被消掉。

Astralium 為可選 external qualification source，不是 runtime dependency。

---

# 二、八字 Project 推導盤面

## 2.1 流年

八字流年以立春交接時刻切換：立春前屬前一干支年，立春後進入新干支年，不以 1 月 1 日切換。

Project 內建太陽視黃經為自包含近似演算法；距離算得交界 15 分鐘內必須產生 boundary warning，使用可信節氣來源覆核後才可做高精度結論。

## 2.2 流月

八字流月採十二節，不以國曆每月 1 日切換：

| 月支 | 起點 | 太陽視黃經 |
|---|---|---:|
| 丑 | 小寒 | 285° |
| 寅 | 立春 | 315° |
| 卯 | 驚蟄 | 345° |
| 辰 | 清明 | 15° |
| 巳 | 立夏 | 45° |
| 午 | 芒種 | 75° |
| 未 | 小暑 | 105° |
| 申 | 立秋 | 135° |
| 酉 | 白露 | 165° |
| 戌 | 寒露 | 195° |
| 亥 | 立冬 | 225° |
| 子 | 大雪 | 255° |

五虎遁：

- 甲己年丙寅起
- 乙庚年戊寅起
- 丙辛年庚寅起
- 丁壬年壬寅起
- 戊癸年甲寅起

## 2.3 流日

固定流程：

**Gregorian effective civil date → Julian Day Number → 六十甲子日序**

Project Bazi flow-time profile 採 23:00 early-Zi：

- 22:59 仍算當日
- 23:00 起八字有效日期前進一天
- 00:00 不 double-rollover

固定索引：

```text
offset = JDN - 11
day_stem_index = offset mod 10
day_branch_index = offset mod 12
```

## 2.4 流時

時支：子 23:00–00:59、丑 01:00–02:59，之後每兩小時一支，亥為 21:00–22:59。

五鼠遁以已完成八字有效日期處理的日干為基準：

```text
時干索引 = (日干索引 mod 5 × 2 + 時支索引) mod 10
```

## 2.5 十神

流年／月／日／時天干十神一律以本命日主為參照，不得把流日天干當成新的日主。

---

# 三、紫微時間能力

## 3.1 流月定位

```text
implemented / stable / default
```

採農曆月。一般月份初一換月；閏月初一至十五歸原月，十六起歸下一月。八字流月採節氣月，兩者不同步不是 bug。

## 3.2 流日定位

```text
implemented / experimental / on_demand
```

流月命宮作農曆初一流日命宮，每日順行一宮，十二宮循環。是否升 Stable 只依版本化 qualification，不因單一事件吻合升級。

## 3.3 流時定位

```text
implemented / experimental / on_demand
rule_version = 1.0-exp
```

固定公式：

```text
flow_hour_index = (flow_day_index + hour_branch_index) mod 12
```

流日命宮作子時流時命宮，之後每個時辰順行一宮。

---

# 四、Ziwei Fine Cycle Stem / Transformation / Flying｜Phase 2B

固定 profile：

```text
ziwei-fine-cycle-lunar-late-zi-v1
rule_version = 1.0-exp
day_boundary = late_zi_forward-v1
```

流月／流日／流時 stem、transformations、flying 均為：

```text
implemented / experimental / on_demand
```

Calendar Resolver 保持 **neutral**；23:00 紫微 effective-day 前進只由 **fine-cycle profile** 套用，不得搬用 Bazi early-Zi policy。

- 月 stem 採農曆月；23:xx 不提前切月。
- 閏月初一至十五沿用原月，十六起 effective month ordinal +1。
- day stem 以 `late_zi_forward-v1` 處理：22:59 舊日，23:00 effective date +1，00:00 不重複前進。
- hour stem 使用 effective Ziwei day stem 起五鼠遁，hour branch 直接採 `CalendarContext.hour_branch`。

Public qualification：

```text
pinned lunar-lite 1d104fff...   18/18 PASS
pinned iztro 814b77e6...        integration PASS
Astralium fine-cycle             PENDING
```

月層另以 pinned `lunar-python==1.4.8` / revision `000c8a3d74eed098d6256a28fdd51b869324c559` 做可重現 month-continuity qualification：**86 month-oracle checks、0 mismatch**；包含 72 個一般農曆月、7 個真實閏月的 day 15 / day 16 共 14 cases，以及十種年干的 ordinal 13 → 次年正月 10/10 property PASS。

`leap_twelfth_month_second_half` 已有外部 month-continuity evidence：真實 1574 閏十二月 day 15 的 Project month source 為 `丁丑`，pinned lunar-python 的下一個實際農曆月為 1575 正月 `戊寅`，Project day 16 effective month source 亦為 `戊寅`。Pinned lunar-lite 對閏十二月下半月仍有 direct-runtime oracle limitation；不得把 lunar-python continuity 說成 lunar-lite 本身已直接驗證。

相鄰 regression 已鎖定一般換月、農曆跨年、閏月 15→16、23:00 不獨立換月、DST responsibility、same local civil time 不 fork、downstream source reuse 與 modular ↔ generated runtime parity。

上述 qualification **不提升 maturity**；Fine Cycle stem / transformations / flying 仍維持 `implemented / experimental / on_demand`，Astralium fine-cycle 仍為 `PENDING`。

Phase 2B 本身不定義 `ziwei.flowing_stars` 的當前 maturity；後續 Phase 2C 為獨立 capability，實際狀態以 `runtime_info` 為準。

---

# 五、Calendar / Location 責任邊界

Location Resolver：出生地名稱 → geocoding candidates → latitude / longitude / IANA timezone / provenance。多個 material candidates 必須 fail closed，不猜地點。

Calendar Resolver：structured local civil datetime + IANA timezone → neutral `CalendarContext`。處理 timezone、DST、Gregorian→Lunar、civil hour branch 與 calendar validation；**Calendar Resolver 不負責真太陽時**，也不決定 Bazi／Ziwei 命理日界。

Bazi Natal Time Adapter 與 Ziwei Natal Time Adapter 再依各自 profile 產生 `bazi_effective_time` 與 `ziwei_effective_time`。

未來事件的時間應使用事件實際發生地與對應體系規則；不得把出生盤真太陽時校正直接套到所有未來事件。

---

# 六、來源與 qualification

## Bazi Natal

公開 oracle：`lunar-python==1.4.8` / reference revision `000c8a3`。公開 qualification 已 PASS；private natal evidence 仍可保持 PENDING，不能因公開 PASS 自動 promotion。

## Ziwei Natal

公開 oracle：pinned `iztro@2.6.0` / revision `814b77e6371e1050cac31bbf674db3c3138fcfde`，另有 Project formula-frozen true-solar boundary cases。Astralium private qualification 只保存 aggregate state/digest，不提交 raw private chart。

Astralium 為可選 external qualification source。Project true-solar output 不得寫成 Astralium output；兩者一致只能說 qualification match。

## Privacy

共用 repo 不提交真實命主的 raw birth input、full address、raw chart 或 external raw payload。只允許公開 synthetic/public cases、opaque case_id、aggregate counts、digest、versions 與 qualification status。

---

# 七、輸出與可重現性

相同 structured input + 相同 profile/rule version 必須產生相同 structured chart；相同 structured natal + 相同 generated date 必須產生 byte-identical canonical Markdown。

Markdown exporter 只序列化已存在的 deterministic facts，不重新排盤、不猜 missing fields、不自動加入格局／喜忌／性格等命理解讀。

Canonical ordering：

- Bazi：year / month / day / hour
- Ziwei palace：Project canonical `PALACE_NAMES`
- stars：catalog sequence
- transformations：祿／權／科／忌

---

# 八、最終原則

Project 可以自行計算，但必須：算法固定、版本固定、輸入清楚、結果可重現、來源可追溯、能力 maturity 清楚、原始資料與 Project 結果分層、計算與命理解讀分層。

輸入不足：**降低精度，不自由補造。**

本命計算：**Project 原生盤面**。

未來時間衍生：**Project 推導盤面**。

資料衝突：**保留 External / Project / Resolved 與 MATCH / EQUIVALENT / CONFLICT / NOT_COMPARABLE。**

---

# 九、Phase 2C Ziwei Flowing Stars

正式 capability：

```text
ziwei.flowing_stars = implemented / experimental / on_demand / 1.0-exp
profile = ziwei-flowing-stars-common-v1
classification = Project 推導盤面
```

Canonical location 只使用地支 `target_branch`；宮名只在 materialized view 出現。`FlowingStarLayer` 與既有 `CycleTransformationLayer` 分離。

五種 scope source adapter 固定為：

- `decadal`：直接使用 `ZiweiDecadalPeriod.stem_branch`，不得重新推大限干支。
- `yearly`：使用 lunar-year neutral stem/branch，不套八字立春年界。
- `monthly`：直接重用 Phase 2B `ResolvedCycleStem`，不得重新推流月干支或 boundary policy。
- `daily`：直接重用 Phase 2B `ResolvedCycleStem`，不得重新推流日干支或 boundary policy。
- `hourly`：直接重用 Phase 2B `ResolvedCycleStem`，不得重新推流時干支或 boundary policy。

核心星曜固定為天魁、天鉞、文昌、文曲、祿存、擎羊、陀羅、天馬、紅鸞、天喜；`yearly` 另加年解。non-yearly 固定10顆，yearly 固定11顆。

Phase 2C v1 不包含歲前十二神、將前十二神、博士十二神、長生十二神、小限流曜、流曜亮度、scoring 或 AI interpretation。

Public qualification 使用 pinned `SylarLong/iztro 2.6.0` revision `814b77e6371e1050cac31bbf674db3c3138fcfde`，600 source cases／6120 placements／0 unexpected mismatch。Astralium flowing-stars private qualification 維持 `PENDING`，不得拿 Natal private PASS 代替。

---

# 十、Historical Activation Selector v1

正式 capability：

```text
historical.activation_selector = implemented / experimental / on_demand / 1.0-exp
profile = historical-activation-bazi-v1
classification = Project 推導盤面
```

## 10.1 責任邊界

Selector 只回答最近歷史流年中「盤面結構互動相對強弱」，不直接預測某年必然離職、結婚、疾病或其他具體事件。

- **Python**：建立最近10個 eligible flow-year periods、計算 evidence、去重／suppression、排序、選出 canonical 4 High + 1 Control、產生 digest。
- **AI**：只能在 Python 選年後解讀可能活躍領域與事件形式；不得選年、換年、重排或用已知歷史事件改 selector truth。
- **使用者**：驗證／訂正實際事件。

Selector payload 不接受 `preferred_years`、`known_event_years`、`event_keywords`、`manual_rank_override` 等 history-based ranking hints。

## 10.2 十年視窗

ranking unit 固定為 Bazi flow-year period，以立春交接時刻為界：

```text
label_year   = 該流年期起始立春所在 Gregorian year
period_start = 該年立春交接時刻
period_end   = 下一年立春交接時刻
```

只取 `as_of_datetime` 前最近10個**已完整結束**的流年期。未走完的當前流年不進第一次 Historical Blind Calibration canonical window。

## 10.3 Tier 定義

方向固定：

```text
Tier 1 = 核心強訊號
Tier 2 = 中度支持訊號
Tier 3 = 輔助訊號
```

Tier 是 activation strength，不是吉凶。

### Tier 1

- `decadal_boundary`：八字大運交界落在該流年期。
- `sui_yun_bing_lin`：流年完整干支與當時大運完整干支相同。
- `natal_pillar_repeat`：流年完整干支伏吟本命任一柱。
- `branch_clash_natal`：流年支與本命任一支六沖。
- `branch_clash_decadal`：流年支與當時大運支六沖。
- `completes_three_harmony`：流年支加入後補成完整三合。
- `completes_three_meeting`：流年支加入後補成完整三會。
- `completes_three_punishment`：流年支加入後補成寅巳申或丑戌未完整三刑。

### Tier 2

- `branch_six_harmony`：六合。
- `partial_three_harmony`：三合半組／待補完整。
- `partial_three_meeting`：三會半組／待補完整。
- `branch_punishment_support`：pairwise 刑／自刑。
- `branch_repeat`：單支伏吟，且未被完整柱伏吟／歲運並臨吸收。
- `stem_combination`：天干五合。

### Tier 3

- `branch_harm`：六害。
- `branch_break`：六破。
- `stem_repeat`：天干同干，且未被完整柱伏吟／歲運並臨吸收。

固定 relation tables 由 `engine/historical/relations.py` version control；一般五行生剋、十神名稱、喜用神、格局分數、神煞、天干沖與 AI 主觀重要度不進 v1 ranking。

## 10.4 Suppression / dedup

同一結構不得灌水：

- 歲運並臨吸收同一 decadal target 的 lower-tier stem/branch repeat。
- 本命完整柱伏吟吸收同一 natal component 的 lower-tier stem/branch repeat。
- 完整三合／三會／三刑形成時，對應 partial pattern 不再重複計數。
- 同一 canonical pattern 即使本命有重複支，也只記一組 pattern evidence。

## 10.5 Ranking

v1 不使用0～100黑箱分數。每年建立 rank vector：

```text
tier1_family_count
tier1_evidence_count
cross_layer_tier1
tier2_family_count
tier2_evidence_count
tier3_family_count
tier3_evidence_count
label_year
```

由上到下 lexicographic descending。`label_year` 只在盤面 evidence 完全同分時作 deterministic tie-break，較近年份優先。

真正排序完成後：

```text
High Years = Top 4
Control    = Bottom 1
```

不得為了年份分散、避免連續、跨大運、聊天內容或已知事件而人工換年。

## 10.6 Control quality

```text
strong_control:
  Tier 1 family = 0 且 Tier 2 family = 0

acceptable_control:
  Tier 1 family = 0 且 Tier 2 family > 0

relative_low:
  Tier 1 family > 0
```

`relative_low` 只能稱「近十年相對最低」，不得說成真正穩定年。

## 10.7 Bazi ranking authority / Ziwei support-only

v1 的 canonical ranking authority 固定是 Bazi。

Python 先完成 Bazi canonical selection 與 `selection_digest`，之後才可對 canonical 5 年附加 Ziwei yearly context：

```text
ziwei_support.role = support_only
ziwei_support.ranking_authority = false
```

Ziwei support 採各 Bazi flow-year period 的 deterministic midpoint 作 annual context sample，呼叫既有 yearly Ziwei runtime；它只提供 AI 事件領域解讀的第二體系背景。

Ziwei support 缺失、部分失敗或內容改變時：

- 不得改 `high_years`。
- 不得改 `control_year`。
- 不得改 Bazi `selection_digest`。
- Bazi selector 仍可正常輸出，Ziwei support 只標 `partial` / `unavailable`。

未來若要讓 Ziwei 參與 ranking，必須建立新的 versioned cross-system profile 並重新 qualification；不得偷改 `historical-activation-bazi-v1`。

## 10.8 Historical Blind Calibration 時間評價

使用者回報實際事件時，以實際日期映射到 Li-Chun flow-year period；不能只比較 Gregorian year label。

例如預測 `2016流年`，若實際事件發生在2017年1月且仍位於2016立春～2017立春前，時間評價仍是 `exact_flow_year`，不是機械式 `shifted +1`。

若使用者只記得粗略年份，且可能跨立春邊界，標 `unscorable_or_ambiguous`；不得逼使用者亂填月份。

## 10.9 可重現性與版本治理

相同 normalized natal + 相同 `as_of_datetime` + 相同 timezone + 相同 profile/rule version 必須產生相同10年 ranking、canonical 4+1 與 `selection_digest`。

歷史事件 ground truth 不得回流改寫同次 selection。未來修改 Tier family、relation table、suppression 或 ranking priority 時，必須升 profile/rule version，不能靜默改 v1。

---

# 十一、Phase 3.5 Cold-start Structural Interpretation

正式 capability：

```text
distribution.structural_interpretation = implemented / experimental / on_demand
structural_profile = lin_tianji_structural_v1-exp
mapping_profile = lin_tianji_domain_v2-exp
classification = Project 推導盤面
ranking_authority = false
```

Phase 3.5 只把已 materialized 的 deterministic Bazi / Ziwei structural facts 轉成 `EvidenceFeature`。八字使用中立 structural relation truth、當前已存大運、本命四柱與目標 scope；紫微使用 materialized flowing-star category、宮位 ownership、transformation / flying edge 與 geometric provenance。Phase 3 ranker 仍是唯一 ranking authority，`lin_tianji_rank_v1-exp` 的 domain ownership gate、dependency de-dup、specificity ceiling 與 ranking digest 不由本層修改。

三條硬邊界固定如下：

1. **Zero-history usable baseline**：完全沒有 historical event material 時，`resolve_forecast_context → interpret_structural_evidence → rank_evidence` 仍必須產生 deterministic、non-empty、可追溯的 cold-start baseline。
2. **Activation strength ≠ probability / 吉凶**：`weak / moderate / strong` 只代表已版本化 structural activation，不代表事件機率、好壞或必然結果；relation 不得直接翻成離職、破產、結婚等 concrete event。
3. **Historical Personalization only after base ranking**：歷史資料只能在 cold-start evidence 與 Phase 3 base ranking 完成後做個人化排序修正；不得回頭改 deterministic chart、Phase 3.5 structural evidence 或 base ranking digest。

Phase 3.5 public payload 不接受 `historical_records`、`actual_event`、`known_event_years` 等 outcome/history material。已驗證事件不得成為當期訊號來源，也不得拿來調整 closed semantic mapping 或 structural strength class。

`build_evidence_features()` 保留為 compatibility wrapper，但正式 semantic authority 與 `interpret_structural_evidence` 共用同一 v2 Structural Interpretation path；legacy `lin_tianji_domain_v1-exp` 不再是新 prospective path 的正式 authority。

---

# 十二、Phase 4 Historical Personalization

正式 capability：

```text
distribution.historical_personalization = implemented / experimental / on_demand
profile = lin_tianji_historical_personalization_v1-exp
base_ranking_policy = lin_tianji_rank_v1-exp
ranking_authority = false
```

Phase 4 是 **Phase 3 base ranking 完成後**的個人化層。Phase 3 的 `ranking_digest`、domain ownership、dependency de-dup、specificity ceiling 與 base ordinal score 都保持 immutable；Phase 4 只能產生另外的 `personalization_digest` 與 bounded personalized ordering。

## 12.1 輸入權威

v1 只接受已完成 finalize 的 Historical Calibration 結構化紀錄。`actual_event`、盲判 hypothesis、研究筆記、自然語言描述與其他自由文字不進 scoring，也不進 historical source identity / personalization digest。

`06` 流年追蹤、`07` 問事追蹤、`08` 重大決策等 prospective outcome tracking 不作 Phase 4 v1 的 historical personalization training input。

## 12.2 Domain 邊界

歷史只能修正 **已由 Phase 3 打開**的 domain：

- history 不得建立 base ranking 中不存在的新 domain。
- 每筆 scoring record 必須只對應一個 canonical domain；multi-domain aggregate 不拆票。
- 同一 `predicted_flow_year` 只取第一筆有效、blind、high-activation record 作 scoring authority。
- contaminated、`cannot_recall`、control 與 unscorable record 不作 domain scoring。

每個 domain 至少需要 2 筆 eligible historical samples 才啟動個人化。結構化 evidence unit 固定：

```text
matched  = +2
partial  = +1
missed   = -2
unscorable = excluded
```

modifier 固定 bounded 在 `-2..+2`；這是 ordinal adjustment，不是機率、勝率或信心百分比。

## 12.3 Event-family 邊界

Event family 只能在該 domain 的 **base `event_families` candidates 內重排**：

- 不得新增 base 沒有的 event family。
- 不得刪除 base candidate。
- 每筆 family scoring record 必須只有一個 canonical family；multi-family aggregate 不拆票。
- 正向 evidence 只可把既有 family 前移；負向 evidence 只可把既有 family 後移。
- `allowed_specificity` 永遠沿用 Phase 3 base row，不因歷史校準提高。

## 12.4 No-op 與可重現性

以下情況必須 identity/no-op，完整保留 cold-start base ordering：

- 沒有歷史紀錄。
- calibration status 為 `uncalibrated`。
- 沒有 eligible history。
- 每個 domain / family 的 eligible sample 未達 2 筆。
- 只有 contaminated、cannot-recall、control、unmapped 或 unscorable material。

相同 immutable base ranking + 相同結構化 finalized history + 相同 profile 必須產生相同 `historical_source_digest`、personalized ordering 與 `personalization_digest`。

## 12.5 治理原則

Phase 4 不具 ranking authority；它不能回寫 `EvidenceFeature`、不能改 Phase 3 `ranking_digest`、不能把歷史事件變成當期盤面訊號，也不能把 activation support 解讀成事件機率。

任何未來要改 evidence unit、sample threshold、modifier cap、canonical alias、year de-dup 或 event-family ordering 規則的變更，都必須升新的 personalization profile version 並重新 qualification，不得靜默修改 `lin_tianji_historical_personalization_v1-exp`。

---

# 十三、Phase 5 Interpretation Contract

正式 capability：

```text
distribution.interpretation_contract = implemented / experimental / on_demand
action = build_interpretation_contract
profile = lin_tianji_interpretation_contract_v1-exp
base_ranking_policy = lin_tianji_rank_v1-exp
ranking_authority=false
PRIMARY_DOMAIN_DISPLAY_LIMIT = 3
```

Phase 5 是 Phase 3 base ranking 之後的 deterministic interpretation boundary，不是第二套排序器。Phase 3 是唯一 base ranking authority；Phase 4 Historical Personalization 與 Phase 5 都只能作用在既有候選與呈現層，不得回寫 Phase 3 `ranking_digest`、ordinal score、domain ownership、maturity 或 event-family candidate set，也不得建立第二套 maturity / specificity。

## 13.1 Canonical input / output

`build_interpretation_contract(base_ranking, anchor, personalization=None, local_windows=None, locked_forecast=None)` 只接受版本化 structured input。Canonical output 至少保存 profile / ranking authority、base ranking digest、anchor、domain interpretation、primary / secondary domains、specificity ceiling、optional personalization / local-window / locked-forecast boundaries、disclosure / strategy policy 與 `interpretation_contract_digest`。

`base_ranking.ranking_digest` 必須先以 canonical JSON 重新驗證；Phase 3 policy 必須是 `lin_tianji_rank_v1-exp`。若帶 personalization，必須驗證其 base digest、candidate identity、allowed specificity 與 Phase 4 authority；若帶 local windows，必須驗證 parent-child digest、scope identity 與 specificity 不得被提升；若帶 locked forecast，必須驗證 lock digest、anchor / cutoff 與 canonical claim shape。任一 mismatch 均 fail closed。

## 13.2 Candidate / specificity authority

Phase 5 不得新增或刪除 Phase 3 domain，也不得新增、刪除或語意猜測 event-family candidates。`PRIMARY_DOMAIN_DISPLAY_LIMIT = 3` 只是 presentation limit，不改 base rank。

每個 domain 的 forecast specificity 只能保留或降低。Local window 或其他 boundary 只能取不高於 parent / base 的 ceiling；不得利用局部 spike、歷史支持或現實背景升級 specificity。Explanation class 只屬 metadata，用來說明可揭露層級，不得改 score、rank、maturity 或 specificity。

## 13.3 Reality / probability boundary

`known reality`、使用者自由文字、已知 outcome 與策略可行性不是 canonical contract input，不進 `interpretation_contract_digest`，也不得回寫盤面或 ranking。Reality context 可以讓 strategy specificity 更具體，但不得改 forecast claim、不得提高 forecast specificity、known reality 不得改寫 prospective hit，也不得把已知事實包裝成預測命中。

Historical Personalization support class、activation class、ordinal modifier、local spike 都不是事件機率、準確率或勝率；Phase 5 不得輸出或推導 probability / accuracy semantics。

## 13.4 Digest / runtime responsibility

Phase 3 `ranking_digest` 保持原 authority；Phase 4 可另有 `personalization_digest`；Phase 5 只建立 `interpretation_contract_digest`。三者不可互相覆寫或冒充。

Runtime capability identity 固定為 `distribution.interpretation_contract`，action 固定為 `build_interpretation_contract`。相同 canonical input + 相同 profile 必須產生 byte-deterministic contract digest；portable bundle 與 modular runtime 必須 parity。

## 13.5 Evidence reuse governance

既有資料只能依 contamination class 使用：

```text
development_contaminated → bug reproduction / compatibility / fail-closed / wording tests only
regression_only → protects legacy behavior, not accuracy evidence
holdout_unseen → offline validation only if untouched by prior tuning
prospective → Phase 6 append-only evaluation after cutoff
```

`development_contaminated` 與 `regression_only` 不得拿來宣稱預測 accuracy；`holdout_unseen` 一旦參與 tuning 就失去 holdout 身分；`prospective` 必須在 cutoff 後 append-only 評估，Stage 2 不得改寫 Stage 1 lock。

Phase 5 不新增任何命理公式。它只治理既有 deterministic forecast context、Phase 3 ranking、Phase 4 personalization 與 AI interpretation 間的 authority / disclosure / strategy boundary。
