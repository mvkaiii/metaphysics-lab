# v1.6 Yearly Ziwei Transformations Y1 — Validation Record

## Status

- Base frozen authority: `eaf03116bf5e02b08b4b51caed2cb2c31f796686`
- Research branch: `research/v1.6-yearly-ziwei-transformations-y1`
- Draft PR: `#219` (`DO NOT MERGE`)
- Semantic qualification anchor: `f20ec931bf868f152226bab831dfa011f905a4f2`
- Full-gate run: `33634251268`, job `100261005527` — **SUCCESS**
- Runtime classification: **Project 推導盤面**
- Research classification: `RETROSPECTIVE_RESEARCH_ONLY`
- Engineering status: `YEARLY_TRANSFORMATION_PIPELINE_QUALIFIED`
- Retrospective status: `RETROSPECTIVE_COVERAGE_IMPROVEMENT_WITH_PRECISION_UNRESOLVED`
- Official paired scoring: **NO**
- Prospective validation: **NOT EXECUTED**
- `promotion_allowed=false`

The semantic qualification anchor is intentionally the code-and-generated-artifact head validated before this documentation close-out commit. Documentation-only successors must still pass CI, but the document does not recursively rewrite itself to name its own commit SHA.

## Change under test

The research branch adds deterministic yearly Ziwei transformation materialization to the existing yearly forecast source. The implementation reuses:

1. the already-resolved yearly lunar-year source;
2. Project natal star locations;
3. the existing ten-stem transformation profile;
4. the existing flying-edge engine;
5. the existing cycle-layer contract.

It does not modify EFA, C2, HCC, HOC, ranking thresholds, or semantic mappings.

## TDD evidence

### RED

Research workflow run `33629877297`, job `100246414890` failed before implementation. Both focused tests failed with `KeyError: 'transformation_layer'`, demonstrating that the frozen yearly forecast scope did not provide the layer.

### GREEN

After the minimal yearly-cycle implementation, research workflow run `33630184848`, job `100247425599` passed the two focused tests.

Expanded integration workflow run `33631085129`, job `100250410670` passed:

- focused yearly transformation tests;
- source-oracle cross-check;
- full `tests.test_distribution_forecast`;
- full `tests.test_distribution_structural_interpretation`;
- Ziwei transformation/flying/composition core regressions;
- deterministic outcome-blind benchmark generation;
- deterministic separated retrospective adjudication.

The final semantic qualification gate at `f20ec931bf868f152226bab831dfa011f905a4f2` passed the same focused/integration boundaries plus the full repository regression and distribution/qualification checks.

## 2025–2027 source-oracle cross-check

The runtime output below is **Project 推導盤面**. The expected tuples come from the existing Astralium-derived `紫微_基礎資料包_2026-08-19.md` and are used only as a validation oracle.

| Year | Project-generated yearly transformations | Astralium-derived oracle | Result |
|---|---|---|---|
| 2025 乙巳 | 天機祿→夫妻宮；天梁權→命宮；紫微科→兄弟宮；太陰忌→財帛宮 | same four star/type/palace edges | MATCH |
| 2026 丙午 | 天同祿→遷移宮；天機權→夫妻宮；文昌科→疾厄宮；廉貞忌→田宅宮 | same four star/type/palace edges | MATCH |
| 2027 丁未 | 太陰祿→財帛宮；天同權→遷移宮；天機科→夫妻宮；巨門忌→夫妻宮 | same four star/type/palace edges | MATCH |

The cross-check test also repeats forecasts and requires deterministic equality.

## Structural-evidence integration check

For the Project 2025 yearly context, the existing structural interpreter now receives the yearly transformation layer without any EFA or mapping change. A direct yearly Ziwei finance target feature is produced with at least:

- `finance / income_assets`
- `finance / constraint_or_friction`

This verifies the intended data flow:

`yearly source → yearly 四化 → natal target palace → structural EvidenceFeature`

It does **not** establish that a specific bonus event is predictable; that is evaluated separately as retrospective family-level coverage.

## Stale generated-distribution incident and resolution

An earlier full-repository gate failed two tests:

- `test_committed_artifacts_equal_deterministic_rebuild`
- `test_historical_selector_request_matches_modular_runtime`

Systematic debugging showed a single root cause: the modular runtime already contained the new yearly `transformation_layer`, while the committed generated `dist/ai/metaphysics_lab.py` still contained pre-change bytes.

The distribution was regenerated only through the repository's canonical `tools/build_ai_distribution.py` builder. A temporary one-shot workflow was used to commit the deterministic generated artifact and was then removed. No prediction mapping, EFA/C2/HCC/HOC authority, ranking threshold, or event adjudication logic was changed to fix this failure.

At semantic qualification anchor `f20ec931bf868f152226bab831dfa011f905a4f2`, both previously failing tests pass, the builder reports `AI distribution is up to date`, and the checkout is clean before regeneration.

## Final full-gate evidence

Workflow run `33634251268`, job `100261005527`, checked out exact SHA `f20ec931bf868f152226bab831dfa011f905a4f2` and completed successfully.

Evidence includes:

- engine change-boundary guard: PASS;
- Y1 focused + source-oracle tests: 44 tests, PASS;
- frozen v1.6 event-family research regressions: 66 tests, PASS;
- frozen Q1/O1/T1/S1/I1 regressions: 75 tests, PASS;
- Lin Tianji v1.5 focused regressions: 303 tests, PASS;
- prediction validation: 12 / 12 scenarios, PASS;
- release-surface validation: PASS;
- deterministic retrospective benchmark v2: PASS;
- full repository regression: **1098 tests, 0 failures**;
- Python compile check: PASS;
- deterministic AI distribution rebuild/check: PASS;
- Bazi flow-day/hour qualification evidence: up to date;
- Ziwei flow-day/hour qualification evidence: up to date;
- Ziwei month-boundary qualification evidence: up to date.

Final full-gate distribution artifact:

- artifact ID: `9848153332`
- artifact name: `v1.6-yearly-transformations-y1-full-gate-f20ec931bf868f152226bab831dfa011f905a4f2`
- uploaded ZIP SHA-256: `22d0166cdcd427ab9cb53f8444227ac46365ee2d3f0aa79a4237061e9fe1155d`

## Governance boundary check

Diff against the frozen authority shows the only production semantic changes are:

- `engine/ziwei/yearly_cycle.py` — new yearly transformation builder;
- `engine/distribution/forecast.py` — minimal yearly wiring.

`dist/ai/metaphysics_lab.py` is a deterministic generated artifact refreshed from those source changes. No EFA/C2/HCC/HOC/mapping/threshold files are modified.

## Interpretation

The engineering hypothesis is qualified at the semantic anchor: yearly Ziwei transformations can be derived deterministically from existing Project inputs, match the available 2025–2027 source oracle, flow into structural evidence, preserve frozen downstream governance, and pass the full repository/distribution qualification gate.

This does **not** authorize model promotion. Retrospective benchmark coverage improves but precision cost remains unresolved, and future prospective validation is still required.