# v1.6 Claim Consumption Private Threshold Governance T1

## Status

T1 是 general-only、prospective-only 的 private release threshold authority。它只消費 aggregate artifacts，不讀 raw oracle、joined private input、candidate case output 或任何 private prose。

正式 policy profile：`lin_tianji_claim_consumption_private_strict_zero_v1`。

Policy freeze：`2026-09-01T14:59:11Z`。

所有九個 release metrics 採 strict-zero；`promotion_allowed=false`。

## Prospective sequence

唯一合法的未來流程：

`T1 policy freeze -> sampling receipt freeze -> new oracle seal -> candidate freeze -> candidate output -> O1 join -> evaluation identity freeze -> Q1 exactly once -> T1 gate`

順序倒置不得事後補救成 prospective evidence。

## Current exposed pair

current exposed pair 在 T1 policy freeze 前已完成 oracle seal 並已暴露 candidate output，因此 permanently prospective-ineligible。T1 不得對該 pair 做 retrospective PASS/FAIL；本 T1 cycle 也不得執行任何 private Q1 evaluator。

## Authority boundaries

- T1 不重新打分 claim-level alignment。
- Q1/O1/C1、Coordination、Phase 3、Interpretation ranking、Hybrid authority 全部 read-only。
- sampling eligibility 是外部 authority；T1 不自行發明最低 case/claim 數。
- artifact schema/digest tamper 必須 fail closed；只有結構可信但 prospective/sampling/identity 不合格的 evidence 才輸出 `INELIGIBLE`。
- private gate `PASS` 不等於 merge/default switch/tag/release；`promotion_allowed=false` 永久保留。
- raw private artifacts 不得進 git；public repo 只允許 general policy 與 aggregate/digest-only evidence。

## Strict-zero metrics

- `cutoff_contamination_count`
- `over_render_count`
- `under_render_count`
- `caveat_omission_count`
- `unnecessary_caveat_count`
- `specificity_overreach_count`
- `specificity_excessive_downgrade_count`
- `partial_alignment_count`
- `missed_alignment_count`

任一 eligible Q1 aggregate metric大於 0 → T1 `FAIL`；全部為 0 → `PASS`。Sampling、identity、sequencing 或 oracle contamination 不合格 → `INELIGIBLE`。
