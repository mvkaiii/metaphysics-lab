# 林氏天機 v1.5｜Phase 0 RED Gate

- Phase：0 / Blind-lock correctness prerequisite
- Base SHA：`ca7f14f05ba3f0c7f9c54422804549f2cad7eb6d`
- Base tree：`932b9bb1ac051ffe3ba00b5aff165d264b6eb480`
- Baseline release：v1.4.0 (`9065fec2a71a1ba07c8e6b7ef2106599d2d28a85`)
- RED exact head：`73f41424f2af1be2cf4c5291151c3739775ccf22`
- RED workflow run：`32989120666`
- RED job：`98242226005`
- RED test file：`tests/test_distribution_blind_sources.py`
- First GREEN implementation head：`9ee5be897d53d62ce5e4c15eb976f9c49ae4dcfc`

## Verified RED

A valid subject-aware Progressive Case has 05 materialized and 00 truthfully records it. Stage 1 blind forecast input supplies exactly 00–04, as required by anti-leak rules. The pre-fix implementation called the complete Case manifest validator and therefore saw the 00 declaration for 05 without receiving 05 content, producing `case_manifest_mismatch`.

Exact RED evidence on Python 3.9.25:

- Bazi flow-day/hour qualification — PASS.
- Ziwei flow-day/hour qualification — PASS.
- Ziwei month-boundary qualification — PASS.
- deterministic AI distribution build + `--check` — PASS.
- focused suite — 118 tests total, exactly 1 failure and 117 passes.
- only failing test: `test_blind_lock_accepts_base_only_input_when_manifest_tracks_progressive_files`.
- exact error: `case_manifest_mismatch`, expecting 05 to be absent from the supplied partial Stage 1 pack even though 00 truthfully marked it materialized.
- `test_blind_lock_rejects_progressive_file_content_in_stage1_sources` — PASS.
- `test_blind_lock_still_rejects_subject_mismatch_in_base_files` — PASS.
- `test_full_case_validation_remains_strict_and_accepts_materialized_05` — PASS.

The workflow stopped after the focused RED failure, so full regression / compile / clean-tree / artifact are intentionally not counted as RED evidence.

## Minimal GREEN implementation boundary

The first implementation adds `engine/distribution/blind_sources.py` and changes only the subject-aware Stage 1 validation call in `engine/distribution/calibration.py`.

The new validator:

- requires exactly canonical 00–04 content;
- rejects any supplied 05–08 content;
- validates required front matter, record type, schema/contract, subject identity and subject-aware filenames for 00–04;
- requires 00 to mark 00–04 materialized;
- accepts either truthful materialized/absent state for each 05–08 manifest entry without reading those files;
- records the progressive manifest state for audit;
- leaves complete `validate_case()` behavior unchanged.

No metaphysics formula, capability maturity, Case Schema, release identity, forecast weighting, Evidence Engine, or v1.5 ranking logic is changed in Phase 0.
