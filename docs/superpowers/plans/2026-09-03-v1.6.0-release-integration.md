# Metaphysics Lab v1.6.0 Release Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish v1.6.0 from the frozen v1.6 product authority plus qualified Y1 while keeping v1 selector/interpretation defaults and preserving prospective research isolation.

**Architecture:** Start from `eaf03116bf5e02b08b4b51caed2cb2c31f796686`, transplant only qualified Y1 production/runtime blobs and their tests, then add versioned v1.6 release/package/workflow/docs surfaces. Do not merge research-only prospective arm-freeze or outcome artifacts. Release gates remain fail-closed and publication happens only after exact-candidate verification.

**Tech Stack:** Python 3.9, unittest/pytest, GitHub Actions, deterministic three-file AI distribution package.

**Spec:** `docs/superpowers/specs/2026-09-03-v1.6.0-release-integration-design.md`

## Global Constraints

- Product base is exactly `eaf03116bf5e02b08b4b51caed2cb2c31f796686`.
- Qualified Y1 source anchor is exactly `f20ec931bf868f152226bab831dfa011f905a4f2`.
- Selector default remains `historical-activation-bazi-v1 / 1.0-exp`.
- Interpretation default remains `lin_tianji_interpretation_contract_v1-exp`.
- Y1 maturity is `Experimental / Project-derived`.
- No outcome read, oracle creation, private scoring, retuning, or prospective-accuracy claim.
- `promotion_allowed=false` remains the research-promotion state.
- Private/outcome artifacts must never enter git or release assets.
- All release PASS claims must name the exact candidate SHA.

---

### Task 1: Preserve qualified Y1 production identity

**Files:**
- Modify: `engine/distribution/forecast.py`
- Create: `engine/ziwei/yearly_cycle.py`
- Modify: `dist/ai/metaphysics_lab.py`
- Modify: `tests/test_distribution_forecast.py`
- Modify: `tests/test_distribution_structural_interpretation.py`
- Create: `tests/test_yearly_ziwei_transformations_y1.py`

**Interfaces:**
- Consumes exact source blobs from Y1 anchor `f20ec931bf868f152226bab831dfa011f905a4f2`.
- Produces the same Y1-enabled yearly forecast context while leaving default selector/interpretation routing unchanged.

- [x] **Step 1:** Create release branch from frozen product base.
- [x] **Step 2:** Transplant exact qualified Y1 blobs by SHA, not by manual reconstruction.
- [ ] **Step 3:** Verify release-branch Y1 production/test blob SHAs equal qualified source blobs.
- [ ] **Step 4:** Run Y1 focused tests and existing forecast/interpretation regressions.

---

### Task 2: Define v1.6 release contract with RED evidence

**Files:**
- Create: `tests/test_v16_release_contract.py`

**Interfaces:**
- Requires package identity `1.6.0` and `Metaphysics-Lab-v1.6.0-User-Package.zip`.
- Requires v1 defaults to remain unchanged.
- Requires versioned v1.6 validation/release workflows and release docs.

- [x] **Step 1:** Add release-contract tests before release implementation.
- [x] **Step 2:** Open temporary Draft RED PR against `main` to invoke existing full validation.
- [ ] **Step 3:** Confirm failure is caused by missing v1.6 package/workflow/docs implementation, not unrelated regression.
- [ ] **Step 4:** Preserve RED run URL/id as TDD evidence.

---

### Task 3: Implement v1.6 package and release-surface tooling

**Files:**
- Modify: `tools/build_release_package.py`
- Create: `tools/run_v16_release_surface_validation.py`
- Create: `tools/validate_v16_sandbox_evidence.py`

**Interfaces:**
- Package contains exactly `metaphysics_core.md`, `metaphysics_lab.py`, `project_instructions.txt`.
- Package builder reports `release_version=1.6.0`.
- v1.6 release-surface validator preserves all v1.5 safety/UX release checks.
- Sandbox validator is a versioned copy of the structured fail-closed validator contract and rejects PENDING/FAIL, SHA/digest mismatch and duplicate rubric fields.

- [ ] **Step 1:** Change package identity constants to v1.6.0 without changing deterministic ZIP semantics.
- [ ] **Step 2:** Add v1.6 release-surface validator as a thin versioned wrapper/implementation over the existing three-asset checks.
- [ ] **Step 3:** Add v1.6 sandbox evidence validator with the same fail-closed semantics as v1.5.
- [ ] **Step 4:** Run `tests/test_v16_release_contract.py` and `tests/test_v15_release_contract.py`.

---

### Task 4: Add v1.6 validation and publication workflows

**Files:**
- Create: `.github/workflows/lin-tianji-v1.6-validation.yml`
- Create: `.github/workflows/release-v1.6.yml`

**Interfaces:**
- Validation checks exact PR/head SHA, Python 3.9, Y1 focused tests, frozen v1 defaults, time-engine qualifications, prediction validation, v1.6 release surface, deterministic package, full regression, compile and clean tree.
- Release workflow runs only on `main`/manual dispatch, validates structured v1.6 sandbox evidence before `gh release create v1.6.0`, and publishes only the deterministic package plus three distribution assets.

- [ ] **Step 1:** Create versioned validation workflow without modifying historical v1.5 workflow.
- [ ] **Step 2:** Create fail-closed release workflow that requires real sandbox evidence.
- [ ] **Step 3:** Assert workflow identities and package names through release-contract tests.

---

### Task 5: Add isolated-sandbox v1.6 evidence kit

**Files:**
- Create: `docs/release/v1.6.0-isolated-sandbox-script.md`
- Create: `tests/fixtures/v1.6.0-isolated-sandbox-fixture.v1.json`
- Create: `tests/fixtures/v1.6.0-isolated-sandbox-evidence.pending.md`

**Interfaces:**
- Reuse the existing v1.5 critical release rubrics rather than inventing weaker criteria.
- Add explicit prompts that Y1 must be described as Project-derived Experimental and must not be presented as prospectively proven.
- PENDING evidence must be rejected by the validator.

- [ ] **Step 1:** Copy the fixed synthetic v1.5 kit into versioned v1.6 identities with no private data.
- [ ] **Step 2:** Add Y1 experimental/no-accuracy-claim checks to the fixed script while retaining the existing critical rubrics.
- [ ] **Step 3:** Commit only PENDING evidence before the actual isolated run.
- [ ] **Step 4:** Run validator and confirm PENDING is rejected.

---

### Task 6: Finalize v1.6 release documentation

**Files:**
- Create: `docs/發布說明-v1.6.0.md`
- Create: `docs/release/v1.6.0-qualification.md`
- Modify after technical gates are green: `VERSION.md`, `CHANGELOG.md`, `README.md`, `docs/更新與版本同步.md`, `docs/快速開始.md`, `docs/安裝到ChatGPT-Project.md`

**Interfaces:**
- Must state `v1.6.0`, Y1 `Experimental / Project-derived`, v1 defaults preserved, `promotion_allowed=false`, and that release does not prove prediction accuracy.
- Historical v1.5.0 identity remains immutable.

- [ ] **Step 1:** Create release notes and qualification template with current factual statuses only.
- [ ] **Step 2:** After all technical gates except the manual isolated run are green, update current-version docs to v1.6.0.
- [ ] **Step 3:** Rebuild AI distribution if any source-distributed docs change.
- [ ] **Step 4:** Re-run release-contract/document consistency tests.

---

### Task 7: Execute exact-candidate qualification and publish

**Files:** no semantic production changes; only real evidence may be added.

**Interfaces:** exact release candidate SHA is immutable for the final qualification cycle.

- [ ] **Step 1:** Run Y1 and v1.6 focused suites.
- [ ] **Step 2:** Run Bazi/Ziwei time-engine qualification checks.
- [ ] **Step 3:** Rebuild/check AI distribution and deterministic v1.6 package.
- [ ] **Step 4:** Run prediction validation and v1.6 release-surface validation.
- [ ] **Step 5:** Run full repository regression and Python 3.9 compile.
- [ ] **Step 6:** Verify clean tree and no private/outcome/oracle/scoring artifacts.
- [ ] **Step 7:** Execute the fixed isolated sandbox against the exact candidate; record real PASS evidence only if all rubrics pass.
- [ ] **Step 8:** Validate real sandbox evidence against exact SHA/digests.
- [ ] **Step 9:** Confirm hosted v1.6 validation is GREEN on the exact candidate.
- [ ] **Step 10:** Mark release PR ready, merge to `main`, verify the merged release workflow, and publish immutable `v1.6.0` tag/GitHub Release.

## Exit Gate

Release only when all are evidenced on the exact candidate/merged release SHA:

```text
Y1 qualified and byte-identical to approved production source
v1 selector default preserved
v1 interpretation default preserved
Y1 labeled Experimental / Project-derived
promotion_allowed=false for research-model promotion
no outcome/oracle/private scoring/retuning contamination
Bazi/Ziwei qualification PASS
prediction validation PASS
v1.6 release surface PASS
v1.6 deterministic package PASS
full regression PASS
Python 3.9 compile PASS
clean tree PASS
isolated sandbox PASS
release/version/docs consistent
```
