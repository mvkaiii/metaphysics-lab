# v1.6.0 Isolated Sandbox Contract v2 Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 將v1.6.0 isolated sandbox從combined v1 fixture升級為staged-visibility v2 contract，並以fail-closed evidence validator與測試保護新的fixture、transcript及post-sandbox seal邊界；本次只建立sandbox-ready candidate，不執行正式sandbox、不填PASS evidence、不merge PR #223、不tag、不release。

**Architecture:** 保留三個正式release assets與production runtime不變，將原synthetic Mina fixture拆成child-safe base、operator-only known reality與operator-only historical ledger三個有獨立version/SHA256的檔案。v2 validator以新的evidence schema綁定四份fixture/script/transcript identity、exact candidate SHA、distribution與User Package digest，並只允許transcript evidence、conversation validation與qualification在sandbox後發生變更。

**Tech Stack:** Python 3.9-compatible standard library、`unittest`、Git commit ancestry/diff、deterministic release-surface validator與User Package builder。

**Spec:** 使用者於2026-09-03/04訊息提供的「v1.6.0 isolated sandbox contract v2 sync」目標設計與限制。

## Global Constraints

- 只操作`mvkaiii/metaphysics-lab`的`release/v1.6.0-integration`；不得碰其他Codex repository或`codex/appsflyer-media-source-registration-timing`。
- 當前遠端branch HEAD已確認為`81b7fab4f4e7aef6f91dffb44fa1fa999bf29ed1`；若修改前重新確認發現不一致，停止，不rebase、merge或reset。
- 不修改Y1算法、Selector defaults、Interpretation defaults或production prediction behavior。
- 不把先前automation sandbox ZIP、人工transcript或既有v1 candidate冒充v2 sealed evidence。
- `docs/release/v1.6.0-isolated-sandbox-conversation-validation.md`在正式v2 sandbox完成前維持`status: PENDING`，不得預填PASS、run metadata、digest或rubric PASS。
- 完整historical ledger、known reality、rubric、script與operator-only資料不得進入受測AI的初始context。
- 不merge PR #223、不tag、不建立GitHub Release、不push。
- 所有新測試必須先以RED證明能捕捉缺口，再以最小實作GREEN；不重寫無關production code。

---

### Task 1: Add v2 contract tests first (RED)

**Files:**
- Create: `tests/test_v16_sandbox_contract_v2.py`
- Modify: `tests/test_v16_release_contract.py`

**Interfaces:**
- Tests import `tools.validate_v16_sandbox_evidence` and exercise `validate_fixture_contract(root)`, `validate_evidence(root, evidence_path, current_sha)` and `_validate_evidence_seal_delta(root, tested_sha, current_sha)`.
- Tests define the v2 canonical paths and build PASS-shaped evidence only from actual repo hashes/digests; no test data comes from the automation ZIP.

- [x] **Step 1: Write the failing tests for fixture separation and v2 identities.**

  Add tests that require:

  ```python
  def test_base_fixture_rejects_combined_known_reality_or_ledger(self):
      with tempfile.TemporaryDirectory() as temp_dir:
          root = Path(temp_dir)
          write_v2_fixture_set(root)
          base = json.loads((root / BASE_PATH).read_text(encoding="utf-8"))
          base["known_reality_context"] = {"contaminated": True}
          (root / BASE_PATH).write_text(json.dumps(base), encoding="utf-8")
          self.assertIn("base_fixture_contains_known_reality_context", validator.validate_fixture_contract(root))

  def test_missing_operator_fixture_hash_fails(self):
      evidence = valid_evidence_fields()
      evidence.pop("known_reality_fixture_sha256")
      report = validate_evidence_with_fields(evidence)
      self.assertIn("known_reality_fixture_sha256_invalid", report["errors"])

  def test_wrong_transcript_hash_fails(self):
      evidence = valid_evidence_fields()
      evidence["transcript_sha256"] = "0" * 64
      report = validate_evidence_with_fields(evidence)
      self.assertIn("transcript_sha256_mismatch", report["errors"])
  ```

  Also require v2 script path/version, three fixture version fields, deterministic package SHA field, transcript reference/SHA field, and the absence of the old v1 script/combined fixture as the authoritative path.

- [x] **Step 2: Add one failing test for each critical evidence/seal rule.**

  Add tests with clear names for:

  ```python
  def test_any_critical_rubric_fail_blocks_release(self):
      evidence = valid_evidence_fields()
      evidence["specificity_pass"] = "FAIL"
      report = validate_evidence_with_fields(evidence)
      self.assertIn("rubric_not_pass:specificity_pass", report["errors"])

  def test_tested_sha_followed_by_non_evidence_change_fails(self):
      tested_sha, current_sha = make_git_history(changed_path="dist/ai/metaphysics_lab.py")
      errors = validator._validate_evidence_seal_delta(root, tested_sha, current_sha)
      self.assertIn("post_sandbox_non_evidence_change:dist/ai/metaphysics_lab.py", errors)

  def test_evidence_only_seal_delta_passes(self):
      tested_sha, current_sha = make_git_history(
          changed_path="docs/release/v1.6.0-isolated-sandbox-transcript.md"
      )
      self.assertEqual(validator._validate_evidence_seal_delta(root, tested_sha, current_sha), [])
  ```

  Use a temporary Git repository for seal-delta tests, with explicit test-only Git identity; do not create commits in the target release repository.

- [x] **Step 3: Run the new tests and confirm the expected RED failures.**

  Run:

  ```powershell
  & $py -m unittest tests.test_v16_sandbox_contract_v2 -v
  ```

  Expected: failures naming missing v2 validator interfaces, v2 paths/fields, and fixture separation; no import or syntax errors. Do not implement production behavior until this RED result is observed.

---

### Task 2: Implement v2 fixture separation and validator core (GREEN)

**Files:**
- Create: `tests/fixtures/v1.6.0-isolated-sandbox-base.v1.json`
- Create: `tests/fixtures/operator-only/v1.6.0-isolated-sandbox-known-reality.v1.json`
- Create: `tests/fixtures/operator-only/v1.6.0-isolated-sandbox-historical-ledger.v1.json`
- Create: `docs/release/v1.6.0-isolated-sandbox-script.v2.md`
- Modify: `tools/validate_v16_sandbox_evidence.py`
- Modify: `tests/fixtures/v1.6.0-isolated-sandbox-evidence.pending.md`
- Modify: `docs/release/v1.6.0-isolated-sandbox-conversation-validation.md`

**Interfaces:**
- `validate_fixture_contract(root: Path) -> list[str]` validates all three fixture files, their exact version strings, JSON shape, and visibility boundary.
- `validate_evidence(root: Path, evidence_path: Path, current_sha: str) -> dict` returns `status`, `release_allowed`, `current_sha`, optional `tested_release_candidate_sha`, and sorted fail-closed `errors`.
- `_validate_evidence_seal_delta(root: Path, tested_sha: str, current_sha: str) -> list[str]` returns no errors for same SHA or evidence-only descendant changes, and returns one error per forbidden changed path.

- [x] **Step 1: Split the canonical Mina values without changing them.**

  Put only `fixture_version`, `classification`, `privacy`, `subject`, and `target_year` in the child-safe base. Put the exact existing `known_reality_context` object in the operator-only known-reality file, and the exact existing 2016–2025 ledger rows in the operator-only historical-ledger file. The base must contain neither `known_reality_context` nor `historical_event_ledger`; the operator files must not add new events or real-person data.

- [x] **Step 2: Add the v2 validator constants and fixture contract.**

  Define these canonical values:

  ```python
  EVIDENCE_SCHEMA_VERSION = "v1.6.0-isolated-sandbox-evidence.v2"
  SCRIPT_VERSION = "v1.6.0-isolated-sandbox-script.v2"
  BASE_FIXTURE_VERSION = "v1.6.0-isolated-sandbox-base.v1"
  KNOWN_REALITY_FIXTURE_VERSION = "v1.6.0-isolated-sandbox-known-reality.v1"
  HISTORICAL_LEDGER_FIXTURE_VERSION = "v1.6.0-isolated-sandbox-historical-ledger.v1"
  SCRIPT_PATH = Path("docs/release/v1.6.0-isolated-sandbox-script.v2.md")
  BASE_FIXTURE_PATH = Path("tests/fixtures/v1.6.0-isolated-sandbox-base.v1.json")
  KNOWN_REALITY_FIXTURE_PATH = Path("tests/fixtures/operator-only/v1.6.0-isolated-sandbox-known-reality.v1.json")
  HISTORICAL_LEDGER_FIXTURE_PATH = Path("tests/fixtures/operator-only/v1.6.0-isolated-sandbox-historical-ledger.v1.json")
  TRANSCRIPT_PATH = Path("docs/release/v1.6.0-isolated-sandbox-transcript.md")
  EVIDENCE_SEAL_ALLOWED_PATHS = frozenset({
      "docs/release/v1.6.0-isolated-sandbox-transcript.md",
      "docs/release/v1.6.0-isolated-sandbox-conversation-validation.md",
      "docs/release/v1.6.0-qualification.md",
  })
  ```

  `validate_fixture_contract` must fail when any required file is missing, any version mismatches, the base contains either forbidden visibility key, or operator fixtures have the wrong top-level shape/version. Keep the release-surface and User Package checks unchanged.

- [x] **Step 3: Upgrade PASS evidence validation to v2 fields and hashes.**

  Require `base_fixture_version`, `known_reality_fixture_version`, `historical_ledger_fixture_version`, `base_fixture_sha256`, `known_reality_fixture_sha256`, `historical_ledger_fixture_sha256`, `transcript_reference`, and `transcript_sha256`, in addition to the existing candidate/distribution/package/script/run metadata fields. Require `transcript_reference` to equal the canonical transcript path and compare all actual SHA256 values before allowing release.

  Keep `status: PENDING` fail-closed and keep the eight existing rubric keys exact. Any rubric other than `PASS`, duplicate rubric, invalid digest, failed release surface, package mismatch, or forbidden post-sandbox path must block release.

- [x] **Step 4: Run the focused tests and confirm GREEN.**

  Run:

  ```powershell
  & $py -m unittest tests.test_v16_sandbox_contract_v2 -v
  ```

  Expected: all new v2 validator/fixture/seal tests pass. Then run the original v1.6 contract test and confirm it is updated to the v2 authoritative paths without changing Y1/default assertions.

---

### Task 3: Complete v2 script, transcript path, qualification bookkeeping and release wiring

**Files:**
- Create: `docs/release/v1.6.0-isolated-sandbox-transcript.md`
- Modify: `docs/release/v1.6.0-qualification.md`
- Modify: `.github/workflows/release-v1.6.yml`
- Modify: `.github/workflows/lin-tianji-v1.6-validation.yml` only if v2 contract tests need explicit workflow coverage
- Modify: `tests/test_v16_release_contract.py`

**Interfaces:**
- The v2 script is the only authoritative sandbox protocol document and names the three release assets plus base fixture for Setup/P01–P04, P05 operator disclosure, and P06 blind year lock followed by requested-year-only disclosure.
- The transcript path is evidence-only and remains a pending/template record until a future genuine v2 run; it contains no prior automation transcript or PASS claim.
- Release workflows validate the v2 evidence path/validator but do not publish anything in this branch.

- [x] **Step 1: Write the v2 script with explicit staged visibility.**

  Preserve the existing P01–P11 prompts and Mina canonical case, while changing the visibility section to state:

  ```text
  Setup/P01–P04: only the three release assets and synthetic base fixture are visible.
  P05: known reality is hidden until the operator reveals the fixed object.
  P06: the full ledger is hidden; the AI must blind-select and lock years first;
  only requested year/status/summary rows may then be disclosed.
  Script, rubric, complete ledger and all operator-only files never enter initial context.
  ```

  Do not include operator-only JSON or full ledger content in the script.

- [x] **Step 2: Add the formal transcript evidence path as pending only.**

  Create a short operator-facing placeholder that identifies the path as the v2 transcript evidence location, states that no formal run has occurred, and does not include the previous automation transcript. Keep the conversation-validation evidence `status: PENDING` and update its schema/field names to v2 without filling any digest or rubric with a fabricated value.

- [x] **Step 3: Update qualification and workflow references.**

  Update the qualification contract wording to describe staged v2 visibility, separated child-safe/operator-only fixtures, exact candidate binding, transcript hash, and evidence-only post-sandbox deltas. Keep `isolated sandbox conversation validation PASS on exact release candidate SHA` unchecked and `status: IN_PROGRESS`/`promotion_allowed=false` unchanged. Update release workflow paths/validator arguments to the v2 authoritative script/transcript/evidence contract without changing the publication command or enabling publication on this branch.

- [x] **Step 4: Run documentation and release-contract tests.**

  Run:

  ```powershell
  & $py -m unittest tests.test_v16_release_contract tests.test_v15_release_contract -v
  ```

  Expected: v1.6 assertions now prove v2 path/version/separation/PENDING state, while all v1.5 tests remain unchanged and pass.

---

### Task 4: Verify the sandbox-ready candidate and commit only after fresh evidence

**Files:**
- No further semantic production changes; only the files listed above may be part of the candidate commit.

**Interfaces:**
- Verification must report exact current candidate SHA after commit separately from the pre-commit SHA used for local checks.
- The formal sandbox evidence remains PENDING; no PASS evidence is generated by this task.

- [x] **Step 1: Run sandbox-validator and release-contract checks.**

  Run the focused v2 tests, `tests.test_v16_release_contract`, `tests.test_v15_release_contract`, and the validator against pending evidence; confirm pending is rejected and no prior transcript is consumed.

- [x] **Step 2: Run release surface and deterministic User Package verification.**

  Run:

  ```powershell
  & $py tools/run_v16_release_surface_validation.py --json
  & $py tools/build_release_package.py --verify --output "$env:TEMP\Metaphysics-Lab-v1.6.0-User-Package.zip"
  ```

  Record the output `distribution_digest` and User Package SHA256. Do not write the generated package into the repository.

- [x] **Step 3: Run Python compile and contamination checks.**

  Run the project-equivalent compile check over `engine`, `tools`, `tests`, and `dist/ai/metaphysics_lab.py`; scan release surfaces for private/outcome/oracle/scoring artifacts and assert only the three formal release assets remain in `dist/ai`.

- [x] **Step 4: Run full repository regression and report environment blockers separately.**

  Run the complete `unittest discover` suite with the available workspace runtime. If dependency/vendor integrity failures remain, do not alter product or vendor files to hide them; report the exact failing categories and seek the required dependency-install authorization or runtime correction before claiming a green full regression.

  Execution result: 990 tests ran with 41 failures and 73 errors. The failures are pre-existing bundled vendor integrity (`lunar-python`/`tzdata`) and missing execution-environment dependencies (`lunar_python`, `geopy`, `timezonefinder`), so this candidate does not claim a green full regression.

- [ ] **Step 5: Review diff, verify branch scope and commit.**

  Confirm `git diff --check`, `git status`, `git diff --stat`, and `git diff --name-only` contain no changes outside this v2 contract scope, and confirm `git diff` has no Y1/Selector/Interpretation/production behavior edits. Commit on `release/v1.6.0-integration` with:

  ```powershell
  git add docs/release tests/fixtures tools/validate_v16_sandbox_evidence.py tests/test_v16_sandbox_contract_v2.py tests/test_v16_release_contract.py .github/workflows/release-v1.6.yml .github/workflows/lin-tianji-v1.6-validation.yml
  git commit -m "test: sync v1.6 isolated sandbox contract v2"
  ```

  Do not push, merge PR #223, tag, or create a GitHub Release.

- [ ] **Step 6: Re-run final checks on the committed tree and report exact identities.**

  Reconfirm current branch, new candidate SHA, clean tree, v2 script SHA256, base/known-reality/ledger fixture SHA256, distribution digest, deterministic User Package SHA256, test results and the exact assets/steps an operator must use for the future genuine v2 sandbox. Explicitly state that sandbox gate remains PENDING and that the prior automation transcript was not used as sealed evidence.

---

## Exit Gate

The task is complete only when the committed branch has:

```text
v2 script and staged visibility contract present
child-safe base contains no known reality or full ledger
known reality and historical ledger are operator-only with independent versions/hashes
v2 evidence schema validates all required identity/digest/metadata/transcript fields
all eight critical rubrics are required PASS for any future release evidence
tested SHA is exact or only followed by explicit evidence-only seal changes
non-evidence post-sandbox changes fail
formal transcript path is allowlisted but still pending
qualification sandbox gate remains unchecked/PENDING
Y1, Selector, Interpretation and production behavior unchanged
focused tests, release contract, release surface, package, compile, contamination and clean-tree checks are evidenced
commit exists on release/v1.6.0-integration; no merge/tag/release/push performed
```
