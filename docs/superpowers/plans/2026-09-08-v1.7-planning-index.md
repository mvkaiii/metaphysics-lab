# Metaphysics Lab v1.7 Planning Index

- Date: 2026-09-08
- Status: PLANNED / NOT IMPLEMENTED
- Approved spec: `docs/superpowers/specs/2026-09-08-v1.7-reliability-guided-inquiry-design.md`
- Planning branch: `plan/v1.7-reliability-guided-inquiry`
- Base design branch: `design/v1.7-reliability-guided-inquiry`

## Execution order

```text
Plan 1  Capability Manifest v1
   ↓
Plan 2  Case Doctor + Reconciliation Planner
   ↓
Plan 3  Prospective Validation 2.0
   ↓
Plan 4  Guided Inquiry
   ↓
Plan 5  v1.7 Integration + Release
```

Plans 1–4 each require their own focused GREEN/review before Plan 5 integrates them. They may be developed on separate feature branches, but integration order above is authoritative because Guided Inquiry depends on capability truth and Case safety state, while release integration depends on all four workstreams.

## Plan files

1. `2026-09-08-v1.7-capability-manifest-v1.md`
   - single capability truth;
   - manifest v1 schema/digest;
   - runtime-info exposure;
   - docs drift guard;
   - distribution parity.

2. `2026-09-08-v1.7-case-doctor-reconciliation.md`
   - read-only Case diagnosis;
   - legacy/canonical classification;
   - exact vs possible duplicate handling;
   - deterministic dry-run reconciliation;
   - no automatic delete/overwrite.

3. `2026-09-08-v1.7-prospective-validation-2.md`
   - frozen v1 lock identity;
   - four validation context classes;
   - versioned v2 claim lock;
   - separate clean/conditional/hidden denominators;
   - retrospective path stays Historical Calibration.

4. `2026-09-08-v1.7-guided-inquiry.md`
   - proactive suggestions;
   - 3–4 suggestions, default 3;
   - substantive question answered before suggestions;
   - suppression/blindness/specificity/capability gates;
   - AI renders natural Traditional Chinese from structured intents.

5. `2026-09-08-v1.7-integration-release.md`
   - version contract;
   - user docs / upgrade surface;
   - deterministic v1.7 User Package;
   - v1.7 CI and release-surface validator;
   - fresh isolated sandbox;
   - formal release gate.

## Normative clarification for Plan 5

For implementation, the following file actions are mandatory even if an earlier wording says “as required” or “create or modify”:

```text
Task 4:
CREATE  tests/test_v17_release_package.py

Task 5:
MODIFY  tests/test_ai_distribution_build.py
MODIFY  tests/test_ai_distribution_bundle.py
```

The tests must explicitly cover v1.7 package identity, generated-asset freshness, and modular/bundled parity for every new public action.

## Spec coverage review

| Approved spec area | Implementation owner | Release gate |
|---|---|---|
| Capability single source of truth | Plan 1 | Plan 5 release-surface validator |
| Case Doctor | Plan 2 | sandbox S08/S09 + release validator |
| Reconciliation dry-run / no delete | Plan 2 | sandbox S08 + no-auto-delete rubric |
| clean prospective | Plan 3 | sandbox S05/S06/S07 + denominator rubric |
| conditional prospective | Plan 3 | sandbox S05 |
| hidden existing reality | Plan 3 | sandbox S06 |
| retrospective calibration | Plan 3 | sandbox S07 |
| Guided Inquiry proactive entry | Plan 4 | sandbox S01 |
| 3–4 / default 3 | Plan 4 | sandbox S01/S03/S04 |
| answer-first for substantive query | Plan 4 | sandbox S02 |
| suppression during BLOCKED/disclosure | Plan 4 | sandbox S09/S10 |
| specificity ceiling | Plan 4 | sandbox S11 |
| Experimental ceiling | Plans 1/4 | sandbox S12 |
| Case Schema remains 1.1 | Plans 2/3/5 | release contract |
| Selector/Interpretation defaults unchanged | Plans 1/5 | release contract |
| no Qimen / no new model promotion | Plan 5 | release docs + contract |

No approved v1.7 MVP requirement is intentionally left without an implementation task or release gate.

## Type/interface consistency review

Canonical names shared across plans:

```text
CAPABILITY_MANIFEST_VERSION = "1.0"
load_capability_manifest(...)
diagnose_case(...)
plan_case_reconciliation(...)
classify_validation_context(...)
lock_prospective_forecast(...)
evaluate_locked_claim(...)
build_validation_context_summary(...)
suggest_inquiries(...)
```

Public runtime action names:

```text
diagnose_case
plan_case_reconciliation
classify_validation_context
lock_prospective_forecast
evaluate_locked_claim
summarize_validation_contexts
suggest_inquiries
```

Note: function `build_validation_context_summary()` is exposed through runtime action name `summarize_validation_contexts`; this distinction is intentional and must be covered by dispatch tests.

## Release boundaries

v1.7 does **not** include:

```text
Qimen engine
Selector v2 default promotion
Interpretation v2 default promotion
Ziwei daily/hourly Stable promotion
Case Schema 1.2 migration
private-outcome-driven tuning
```

## Implementation handoff

No production implementation has been performed by these planning commits. At execution time:

1. create an isolated worktree/feature branch;
2. execute one plan at a time with TDD;
3. stop after each plan’s completion gate for review;
4. only enter Plan 5 after Plans 1–4 are individually GREEN;
5. do not merge/tag/release without explicit user authorization at the final release gate.
