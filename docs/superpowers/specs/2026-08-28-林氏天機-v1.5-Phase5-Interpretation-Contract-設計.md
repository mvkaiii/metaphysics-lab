# 林氏天機 v1.5｜Phase 5 Interpretation Contract 設計

## 1. 目的

Phase 5 建立一個獨立、可稽核、deterministic 的 **Interpretation Contract Layer**，位於 Phase 3 base ranking 與 Phase 4 Historical Personalization 之後，負責把已確定的結構化結果整理成 AI 可安全使用的解讀邊界。

Phase 5 不重新排盤、不建立 EvidenceFeature、不重新 ranking、不產生新的 domain / event family，也不把歷史或現實背景轉成新的盤面訊號。它只回答四件事：

1. AI 應先談哪些已存在的候選。
2. 每個候選最多可以講到多具體。
3. 哪些資料只能當背景、時間窗、個人化順序或策略條件。
4. 一般使用者可以看到哪些判斷理由，哪些內部工程資訊不應直接外露。

Phase 5 profile identity 固定為：

```text
lin_tianji_interpretation_contract_v1-exp
```

本 Phase 不提升任何既有 capability maturity，不建立新的 base ranking authority。

---

## 2. 架構定位

正式資料流：

```text
Deterministic Forecast Context
  → Phase 3.5 Structural Interpretation
  → EvidenceFeature
  → Phase 3 Base Ranking
  → Phase 4 Historical Personalization
  → Phase 5 Interpretation Contract
  → AI Natural-language Interpretation / Strategy
```

### Authority 層級

- **Phase 3**：唯一 base ranking authority。
- **Phase 4**：bounded historical personalization metadata，`ranking_authority=false`。
- **Phase 5**：interpretation / disclosure / strategy boundary，`ranking_authority=false`。
- **AI**：依 contract 做自然語言解讀與策略，不得覆寫上游 deterministic truth。

Phase 5 必須把 Phase 3 與 Phase 4 明確分開。Phase 4 的 `personalized_rank`、preferred family order 只代表歷史校準後的呈現偏好，不等於新的盤面強度，也不得被 AI 表述成「盤面本身更強」。

---

## 3. 核心 invariants

Phase 5 必須遵守以下硬規則：

1. `base_ranking.ranking_digest` 必須驗證成功。
2. 若提供 personalization，`personalization.base_ranking_digest` 必須與 base ranking digest 完全一致。
3. Phase 5 不得新增 base ranking 中不存在的 domain。
4. Phase 5 不得新增或刪除任一 domain 原有的 event-family candidate set。
5. Phase 5 不得修改 Phase 3 ordinal score、base rank、EvidenceFeature、dependency relation 或 structural truth。
6. Phase 5 不得提高 `allowed_specificity`；canonical specificity 只能保留 Phase 3 值，或套用 Phase 3 已產生的 local-window cap 進一步降低。
7. Phase 5 不建立第二套 maturity / specificity scoring policy；maturity 只可作 explanation metadata，不重新計分。
8. Phase 4 personalization 不得升高 specificity。
9. 現實背景不得改變 base ranking、personalization、local-window classification 或 forecast confidence。
10. cutoff 前已知事件不得被改寫為 prospective hit。
11. Stage 2 歷史校準不得回寫 Stage 1 blind forecast。
12. Phase 5 不輸出事件機率、命中率或 accuracy estimate。
13. 相同 canonical inputs 必須產生相同 `interpretation_contract_digest`。

任何輸入違反上述 authority relationship，Phase 5 必須 fail closed，不由 AI 猜測或自動修正。

---

## 4. Public Interface

公開函式固定為：

```python
build_interpretation_contract(
    base_ranking,
    anchor,
    personalization=None,
    local_windows=None,
    locked_forecast=None,
) -> dict
```

### 必要輸入

#### `base_ranking`

Phase 3 `lin_tianji_rank_v1-exp` 的 immutable ranking snapshot。

Phase 5 需要驗證：

- policy version
- target scope
- ranking digest
- `evaluated_features`
- opened domain set
- domain rows
- event-family candidate sets
- allowed specificity

`evaluated_features` 只能作 explanation classification 的 read-only source，例如判斷 selected support 是否全為 experimental；Phase 5 不得用它重新計算 eligibility、score、rank 或 specificity。

Phase 5 不直接消費 raw pre-ranking EvidenceFeature 重新排名。

#### `anchor`

Phase 1 已 resolve 的 Query Anchor / prospective window。

至少綁定：

- `query_anchor_at`
- `knowledge_cutoff_at`
- `prospective_window_start`
- `prospective_window_end`
- `query_timezone`
- `question_reference`
- `status`

Query Anchor 只決定知識截止點與 prospective 時間窗，不影響命理 activation、rank 或 strength。

### 可選輸入

#### `personalization`

Phase 4 `lin_tianji_historical_personalization_v1-exp` output。

若不存在，Phase 5 正常產生 cold-start contract。

若存在，必須驗證：

- profile version
- personalization digest
- `base_ranking_digest`
- target scope
- domain set equality
- base event-family candidate set equality
- allowed specificity 不高於 base

Phase 5 只可使用：

- `personalization_status`
- `personalized_rank`
- `historical_support_class`
- `personalized_event_family_order`
- `preferred_event_families`
- `deprioritized_event_families`

Phase 5 不把 Phase 4 modifier 數值或歷史支持 class 翻譯成事件機率。

#### `local_windows`

只接受 Phase 3 `detect_local_spike()` 已產生的 structured rows。

Phase 5 不自行重新判定 local spike / active window。

若提供，至少驗證：

- parent / child scope relationship
- `parent_ranking_digest`
- `child_ranking_digest`
- supplied base ranking 是否對應 target-side digest
- source specificity
- capped allowed specificity
- window type

Local-window evidence 只能維持或降低 specificity，不能升級。

#### `locked_forecast`

只接受 Phase 1 `lock_prospective_forecast()` 的 immutable locked object：

```text
status: locked
method_version
anchor
claims[]
canonical_digest
```

Phase 5 必須重新驗證 `canonical_digest`，並驗證 locked forecast 中的 anchor 與 supplied `anchor` 完全一致。

Phase 5 不接受未鎖定的任意 claim list 冒充正式 claim boundary，也不得修改：

- prediction
- forecast window
- matched_if
- not_matched_if
- knowledge cutoff
- contamination state
- evaluation eligibility
- method version

Locked forecast 只用來告訴 AI 哪些 prospective claims 已正式凍結，以及哪些內容不能因後續現實資料而改寫。

---

## 5. Output Contract

固定輸出包含：

```text
profile_version
target_scope
knowledge_cutoff_at
prospective_window_start
prospective_window_end

base_ranking_digest
personalization_digest
personalization_status

primary_domains
secondary_domains
domain_interpretation[]
local_windows[]

specificity_ceiling
reality_context_policy
forecast_strategy_policy
disclosure_policy
claim_boundaries[]

interpretation_contract_digest
```

### `domain_interpretation[]`

每個 base domain 至少包含：

```text
primary_domain
base_rank
presentation_rank
personalization_applied
base_allowed_specificity
effective_specificity
event_family_candidates
personalized_event_family_order
preferred_event_families
deprioritized_event_families
evidence_explanation_classes
```

#### `base_rank`

固定來自 Phase 3，不變。

#### `presentation_rank`

若 Phase 4 personalization `applied`，可使用 personalized order；否則等於 base order。

這只是 UX presentation order，不建立新的 ranking authority。

#### `event_family_candidates`

必須與 Phase 3 base candidate set 完全相同。

Phase 4 / Phase 5 都不能新增或刪除。

#### `personalized_event_family_order`

若 personalization 有效，可改順序，但集合必須與 base candidate set 完全相等。

---

## 6. Primary / Secondary Domain 規則

`primary_domains` / `secondary_domains` 是 **UX organization**，不是新的 scoring model。

Phase 5 不增加新的命理 threshold、不重新計分，也不使用隱藏 probability cutoff。

Phase 5 v1 固定：

```text
PRIMARY_DOMAIN_DISPLAY_LIMIT = 3
```

規則：

- `domain_interpretation` 永遠保留所有 Phase 3 opened domains。
- `primary_domains` 依 presentation order 取前 3 個。
- 其餘全部進 `secondary_domains`。
- 若 opened domains 少於 3 個，全部都是 primary。
- 展示上限 3 屬 UX / response organization policy，不得被描述為「只有前三名才有命理作用」。
- secondary domain 仍完整保留在 contract 中，使用者追問時可展開。
- `PRIMARY_DOMAIN_DISPLAY_LIMIT` 不參與 Phase 3 score、eligibility、ranking digest 或 Phase 4 modifier。

---

## 7. Specificity Ceiling

Phase 5 canonical effective specificity 固定為：

```text
effective_specificity = 最保守值(
    Phase 3 allowed_specificity,
    applicable Phase 3 local-window allowed_specificity
)
```

若沒有 applicable local window：

```text
effective_specificity = Phase 3 allowed_specificity
```

Phase 5 不自行增加新的 maturity cap，也不重跑 Phase 3 `_allowed_specificity()` 邏輯。Maturity 可用於 explanation class，但不能形成第二套 specificity authority。

### 範例

Phase 3：`concrete_event`

Daily local spike：`allowed_specificity = event_family`

Phase 5 effective specificity 必須是 `event_family`。

Phase 3：`event_family`

即使 Phase 4 歷史 repeated support 很強，也不能升成 `concrete_event`。

### 全域 `specificity_ceiling`

`domain_interpretation[].effective_specificity` 才是每個 domain 的正式上限。

頂層 `specificity_ceiling` 只是 UX summary，等於所有 domain effective specificity 中最高可用層級；它不得覆蓋或提高任何個別 domain 的限制。

### 解讀語意

- `domain`：只能談領域或主題。
- `event_family`：可談事件類型或落地形式。
- `concrete_event`：允許較具體事件候選，但仍不是「一定發生」。

任何 Phase 5 contract 都不得把 specificity level 包裝成 probability。

---

## 8. Evidence Explanation Classes

Python 不直接生成最終自然語言回答，只產 deterministic explanation classes。

Phase 5 v1 固定支援：

```text
independent_convergence
single_system_support
target_with_background_modifier
background_modifier
local_spike
active_window
historically_supported_order
historically_mixed_order
historically_contradicted_order
experimental_only
```

Explanation classes 只能從已驗證的 Phase 3 ranking metadata、Phase 4 personalization metadata 與 Phase 3 local-window rows映射，不重新計 score。

### 語意

#### `independent_convergence`

Phase 3 已記錄多組相對獨立 evidence dependency 同時支持同一 domain。

對外可說：

> 這個主題不是只靠單一訊號，而是有多組相對獨立的盤面依據共同指向。

不得對外列 raw bonus / weight。

#### `single_system_support`

主要由單一體系或單一獨立來源支撐。

不得自動標成低可信；只說證據來源較集中。

#### `target_with_background_modifier`

Target-period evidence 已開啟 domain，同時存在較粗時間層 background modifier。

#### `background_modifier`

較粗時間層只能當背景，不能單獨製造 target-period concrete event。

#### `local_spike`

較細時間層局部變強，但不改寫整體週期主軸。

#### `active_window`

父子時間層方向一致，表示既有主題進入較活躍時間窗。

#### `historically_supported_order`

歷史校準支持某種既有落地形式，因此呈現順序可優先。

不得說成「發生機率較高 X%」。

#### `historically_mixed_order`

歷史落地形式不一致，個人化只能弱化使用。

#### `historically_contradicted_order`

歷史結構化校準對某既有候選形成 bounded deprioritization；候選仍存在，不得刪除。

#### `experimental_only`

由 Phase 3 `evaluated_features` 的 read-only maturity metadata 判定所引用 target support 全為 experimental。這只影響解釋措辭，不重新計算 rank 或 specificity。

對外可說：

> 這一層目前可以作輔助觀察，但不適合把它說成已被高度驗證的具體事件判斷。

---

## 9. Reality Context Policy

Phase 5 contract 不保存使用者現實背景自由文字，也不把自由文字納入 interpretation contract identity。

Contract 只輸出政策：

```text
reality_context_policy:
  may_adjust_strategy: true
  may_adjust_priority_of_advice: true
  may_adjust_feasibility: true

  may_change_base_ranking: false
  may_change_personalization: false
  may_open_domain: false
  may_add_event_family: false
  may_raise_specificity: false
  may_relabel_known_fact_as_prediction: false
```

### Known reality 定義

任何在 `knowledge_cutoff_at` 前已知的事實、安排或已進行事項都是 reality context，例如：

- 已開始工作或任務
- 已存在的合作
- 已知轉職／離職安排
- 已簽合約
- 已確定旅行
- 已知醫療狀況
- 使用者已明確提供的當前計畫

這些資訊可以讓策略更實際，但不能成為 prospective evidence，也不能計算為 prospective hit。

---

## 10. Forecast / Strategy Separation

Phase 5 必須明確區分：

```text
Forecast = 上游 deterministic ranking / windows / locked forecast 所允許的前向判斷
Strategy = Forecast × Reality Context × Feasibility / Constraints
```

固定 contract：

```text
forecast_strategy_policy:
  forecast_specificity_authority: phase3
  strategy_specificity_authority: reality_context
  strategy_must_not_be_presented_as_forecast: true
  known_reality_must_not_be_scored_as_hit: true
  stage2_must_not_rewrite_stage1: true
```

### Strategy 可以比 forecast 更具體

若盤面只允許 `event_family`，forecast 不得越級講某個具體專案一定發生。

但如果使用者現實上已經有一個具體專案，策略可以針對該專案給非常具體的行動建議。

例如：

- Forecast：近期工作／責任與外部合作主題活躍。
- Reality：使用者已在執行 GA4 / GTM 類合作。
- Strategy：建議把一次性協助整理成需求表、事件命名規範、驗收清單與報價流程。

最後一段是策略，不是盤面直接預測。

---

## 11. Stage 1 / Stage 2 Interpretation Boundary

Phase 5 必須保留雙階段問事：

### Stage 1

```text
Phase 3 base ranking
+ anchor
+ local windows
+ optional locked forecast
→ cold-start interpretation contract
```

Stage 1 不讀 Historical Calibration outcome content 來改寫盤面。

### Stage 2

```text
同一份 immutable Phase 3 base ranking
+ Phase 4 personalization
+ 同一 anchor / locked forecast boundary
→ calibrated interpretation contract
```

Stage 2 可以：

- 調整 presentation order
- 說明哪些既有 family 過去較符合這位命主的落地形式
- 提高或降低策略上的觀察優先度

Stage 2 不可以：

- 改寫 Stage 1 原文／prediction
- 新增 Phase 3 不存在的 candidate
- 把已知歷史事件包裝成 Stage 1 原本預測到
- 提高 allowed specificity

---

## 12. Strategy Context State Boundary

概念上策略背景可分：

```text
none
partial
sufficient
```

這不是命理 confidence，也不是 forecast strength。

但 **Phase 5 v1 canonical Python contract 不輸出 `strategy_context_state`，也不接收 reality-context 自由文字做自動分類**。

原因：目前沒有獨立、可重現的 structured reality-context layer。若現在把自由文字交給 Phase 5 分類，會把 semantic interpretation 混入 deterministic contract identity。

Phase 5 v1 只輸出 `reality_context_policy` / `forecast_strategy_policy`。AI 可依當次明確現實背景給策略，但不得把這個臨時判斷寫回 ranking 或 contract digest。

未來若另建 structured context layer，可再新增 closed enum state；不得在 Phase 5 v1 偷做 free-text classifier。

---

## 13. High-Risk Decision Boundary

醫療、法律、保險、稅務、房產、大額投資、高槓桿等領域，Phase 5 只能提供：

- 時間壓力
- 決策節奏
- 盤面傾向
- 心理／資源配置風險
- 防守／進攻條件
- 第二意見、降槓桿、延後、先確認條款等策略

不得把命理解讀替代醫師、律師、會計師、理財／房產專業意見。

此政策沿用現有 Project governance，不建立新的醫療／法律判定引擎。

---

## 14. Disclosure Policy

Phase 5 固定分三種 disclosure level：

```text
user_safe
technical_rationale
internal_only
```

### `user_safe`

一般命理對話預設層級。

可揭露：

- 哪些領域較活躍
- 單一或多組相對獨立訊號
- 全期主題或局部時間窗
- 歷史校準是否提供個人化順序參考
- 最多可以講到哪個 specificity 層級的白話意思
- 為什麼某判斷需要保守

不可直接暴露 raw runtime / schema / scoring field names。

### `technical_rationale`

使用者主動詢問方法、判斷原理、可稽核理由時可使用。

可說明：

- target-period ownership
- background modifier
- fine-time local window
- dependency de-dup 的高層原理
- independent convergence 的高層原理
- Historical Calibration 只能 narrowing / reordering existing candidates
- known reality 與 prospective forecast 分離

仍不預設公開精確 weight、threshold、feature vector 或 anti-gaming mechanics。

### `internal_only`

Repo 維護／技術稽核可用，一般使用者回答預設不直接曝光：

- exact numeric weights
- exact numeric thresholds
- raw ordinal contribution formula
- modifier cap calculation mechanics
- dependency de-dup implementation details
- raw feature vectors
- raw gate formula
- anti-gaming mechanics
- internal digest / field names（除非明確技術稽核）

### Contract schema

```text
disclosure_policy:
  user_facing_language: zh-Hant-TW
  default_explanation_level: user_safe

  allow_high_level_method_rationale: true
  allow_metaphysical_terms_when_relevant: true

  expose_numeric_weights: false
  expose_numeric_thresholds: false
  expose_internal_feature_vectors: false
  expose_raw_gate_formula: false

  expose_ranking_digest_to_ordinary_user: false
  expose_runtime_field_names_to_ordinary_user: false

  technical_audit_may_use_internal_terms: true
```

---

## 15. User-Facing Language Contract

一般使用者回答固定：

- 台灣繁體中文
- 自然完整中文句子
- 先把命理訊號翻成現實語意
- 不必要的英文改成自然中文
- 不把工程欄位當命理結論
- 不用 probability 語言描述 ordinal / historical support

### 一般回答不應直接出現的工程詞

例如：

```text
activation high
maturity experimental
specificity ceiling
eligibility gate
dependency family
ordinal score
historical_modifier_scaled
ranking_digest
runtime validator
```

應翻譯成自然中文。

例如：

- `experimental_only` →「這一層目前可以作輔助觀察，但不足以單獨支持高度具體的事件判斷。」
- `local_spike` →「這比較像局部時間窗突然變強，不代表整體主軸跟著改變。」
- `historically_supported_order` →「過去校準紀錄較支持這種落地方式，因此這次可以優先觀察。」

### 命理術語不是工程術語

八字／紫微的正式術語可依使用者偏好使用：

- 十神
- 五行
- 干支
- 合沖刑害
- 命宮／身宮
- 星曜
- 四化／飛化
- 大運／大限

白話模式先翻譯現實意思；白話＋命理邏輯模式可補術語與依據。

---

## 16. 林氏天機 Branding

「林氏天機」定位為 **method identity**，不是每句命理解讀的行銷前綴。

品牌應出現在：

- capability / profile identity
- 發行文件
- 技術方法說明
- 正式追蹤／評估紀錄的 method version
- 使用者主動問系統方法名稱時

一般命理回答不需要反覆寫「林氏天機指出……」。

可在必要時自然說明：

> 目前這套前向判斷使用的是「林氏天機 v1.5」實驗版方法。

但品牌不能被用來增加神秘感、權威感或宿命感。

---

## 17. Historical Personalization ≠ Probability

Phase 4 support classes：

```text
insufficient
contradicted
mixed
supported
strongly_supported
```

Phase 5 不得翻譯為：

- 80% 機率
- 高機率發生
- 準確率上升多少
- 過去命中率

除非未來 Phase 6 有正式 prospective holdout 與獨立 evaluation contract，否則都不得建立 accuracy claim。

對外應使用：

> 過去的校準紀錄較支持這種落地方式。

而不是：

> 這件事發生機率比較高。

---

## 18. Claim Boundaries

若 supplied `locked_forecast` 存在，Phase 5 輸出 `claim_boundaries[]`，至少保留：

- claim id
- forecast window
- domain
- event family
- contamination state
- evaluation eligibility
- method version
- locked forecast `canonical_digest` reference

Phase 5 不重新 render 或修改 prediction text 作為 canonical claim。

使用者已知事實若與 claim 有關，只能放入策略或後續 Phase 6 evaluation，不得修改 locked forecast。

若沒有 locked forecast，`claim_boundaries` 固定為空陣列，不影響其他 interpretation contract 功能。

---

## 19. Digest Identity

`interpretation_contract_digest` 以 canonical JSON SHA-256 計算。

Digest identity 包含：

- Phase 5 profile version
- target scope
- anchor / cutoff structured fields
- base ranking digest
- personalization digest / status（若有）
- validated local-window structured rows
- deterministic domain interpretation rows
- specificity summary
- policy blocks
- locked forecast canonical digest / claim-boundary structured identities（若有）

Digest 不包含：

- 使用者現實背景自由文字
- Historical Calibration actual-event free text
- AI 最終自然語言回答
- research notes
- transient UI copy

因此自由文字換句話說，不應改變 Phase 5 contract identity。

---

## 20. Fail-Closed Conditions

至少以下情況直接回傳 `DistributionError`：

1. base ranking policy 不支援。
2. base ranking digest mismatch。
3. anchor malformed / cutoff relationship invalid。
4. personalization profile 不支援。
5. personalization digest mismatch。
6. personalization base digest 不等於 supplied base ranking digest。
7. personalization domain set 與 base 不同。
8. personalization event-family set 與 base 不同。
9. personalization 提高 allowed specificity。
10. local-window parent / child digest 與 supplied ranking 不一致。
11. local-window 提高 source specificity。
12. locked forecast canonical digest mismatch。
13. locked forecast anchor 與 supplied anchor 不一致。
14. locked forecast method identity / cutoff 不合法。
15. unknown authority override fields 出現在 runtime payload。
16. 非 deterministic / non-JSON material 被塞入 canonical contract input。

Fail closed 時 AI 不得自己跳過錯誤繼續稱已完成正式 Phase 5 contract。

---

## 21. Runtime / Capability Integration

新增 capability：

```text
distribution.interpretation_contract
```

Metadata 固定：

```text
implementation: implemented
maturity: experimental
routing: on_demand
rule_version: lin_tianji_interpretation_contract_v1-exp
ranking_authority: false
output_classification: Interpretation contract metadata
```

新增 runtime action：

```text
build_interpretation_contract
```

Runtime payload 採 closed contract，只接受：

```text
base_ranking
anchor
personalization
local_windows
locked_forecast
```

`base_ranking` / `anchor` 必填；其他欄位可 absent / null。未知欄位 fail closed。

Runtime wrapper 不自行產生 reality-context semantic classification，也不接收一般使用者自由文字來重新 ranking。

---

## 22. Core AI Rules / UX Docs Integration

Phase 5 需要更新但不推翻現有：

- `core/核心提示詞.md`
- `core/命理分析作業規範.md`
- `core/AI工作流程.md`

重點新增：

1. Interpretation Contract 是 prospective 解讀的 structured boundary。
2. Phase 3 ranking authority 不可被 Phase 4 / 5 覆寫。
3. Stage 1 / Stage 2 forecast boundary。
4. known reality 只能改策略，不得改 ranking / forecast。
5. disclosure levels 與一般使用者語言規範。
6. Historical Personalization 不得被表述成 probability / accuracy。
7. 林氏天機只作 method identity，不作每句話的權威前綴。

現有「台灣繁中、自然語言、隱藏 runtime/schema/materialize、白話優先」契約維持，Phase 5 只補缺口，不做無關大改版。

Generated distribution 只透過 `tools/build_ai_distribution.py` 產生，不手改 `dist/ai/*`。

---

## 23. Test Strategy

新增：

```text
tests/test_distribution_interpretation_contract.py
tests/test_distribution_interpretation_contract_runtime.py
tests/test_distribution_interpretation_contract_bundle.py
```

擴充既有：

```text
tests/test_project_ux_contract.py
tests/test_ai_contract_docs.py
tests/test_distribution_runtime_info.py
```

### 必測 invariants

1. base ranking digest mismatch → fail closed。
2. Phase 4 base digest mismatch → fail closed。
3. personalization 不能新增 domain。
4. personalization 不能新增／刪除 family。
5. personalization 不能提高 specificity。
6. no personalization → deterministic cold-start contract。
7. personalization no-op → presentation order 與 base 一致。
8. personalization applied → presentation order 可變，但 base rank 保留。
9. primary domains 固定最多 3 個，且完整 domain set 仍留在 `domain_interpretation`。
10. primary/secondary 分組不改 base ranking digest。
11. local spike 只能降 specificity。
12. active window 不被誤標 local spike。
13. Phase 5 不重新計 maturity specificity cap。
14. `experimental_only` 只改 explanation metadata，不改 rank。
15. reality-context policy 永遠不可改 ranking。
16. strategy 可更具體，但 contract 明確禁止說成 forecast。
17. Stage 2 不能改寫 Stage 1 locked forecast boundary。
18. unlocked arbitrary claim list 不可冒充 locked forecast。
19. locked forecast digest / anchor mismatch → fail closed。
20. `historically_supported_order` 不得輸出 probability wording。
21. explanation output 不含 numeric policy weights / thresholds。
22. ordinary-user disclosure 不暴露 internal field names。
23. technical rationale 允許高層可稽核理由。
24. user-facing language rule明確要求台灣繁中與自然中文。
25. method identity 包含林氏天機，但普通解讀不要求每段 branding。
26. free-text reality / actual-event rewrite 不影響 contract digest。
27. same canonical input → same digest。
28. modular runtime ↔ generated portable bundle parity。

測試資料全部使用 synthetic / outcome-free fixtures，不拿 Kai 2026 已知結果調 threshold 或 expected ranking。

---

## 24. Historical / Old-Data Test Protocol Boundary

使用者之後提供舊資料時，正式分類至少分成：

```text
development_contaminated
regression_only
holdout_unseen
prospective
```

### `development_contaminated`

已看過結果，可用於：

- bug reproduction
- compatibility
- fail-closed boundary
- wording / contamination test

不得用於調整 mapping / threshold 後再宣稱準確率提升。

### `regression_only`

鎖定既有行為，防止新版讀壞舊資料；不作 accuracy evidence。

### `holdout_unseen`

在 policy / threshold 定稿前沒有被拿來調參的資料，才可作較強的離線驗證。

### `prospective`

knowledge cutoff 後才發生的真正前向紀錄，交由 Phase 6 append-only evaluation。

Phase 5 不建立 accuracy denominator。

---

## 25. Phase 5 Acceptance Criteria

Phase 5 完成前必須證明：

1. Phase 3 仍是唯一 base ranking authority。
2. Phase 4 只能調整既有候選的呈現順序。
3. Phase 5 不產生新 domain / event family。
4. Phase 5 永遠不能提高 specificity。
5. Phase 5 不建立第二套 maturity specificity policy。
6. local-window cap 可正常降低 specificity。
7. 現實背景可以讓策略更具體，但不能改 forecast。
8. 已知事件不能變成 prospective hit。
9. Stage 2 不能改寫 Stage 1 locked forecast。
10. 一般使用者看不到不必要的 internal engineering terms。
11. 使用者仍可取得高層、可稽核的方法理由。
12. Historical Personalization 不被表述成 probability / accuracy。
13. 林氏天機 branding 作 method identity 而非權威話術。
14. 相同 canonical inputs 產生相同 interpretation contract digest。
15. Runtime modular / portable bundle parity 全綠。
16. Existing full regression、Python 3.9 compile、deterministic build/check、clean-tree 全綠。

Phase 5 完成後停在 merge-ready，未經使用者明確授權不得合併。

---

## 26. Out of Scope

Phase 5 明確不做：

- 新 Bazi / Ziwei 排盤公式
- 新紫微流月／流日／流時算法
- ranking weight tuning
- event probability model
- Bayesian / ML personalization
- free-text historical semantic classifier
- reality-context 自動 embedding / LLM classification
- strategy-context automatic classifier
- accuracy improvement claim
- prospective evaluation / failure attribution（Phase 6）
- release / maturity promotion
- cat-eye tuning loop

---

## 27. 與 2026-08-26 Roadmap 的差異

8/26 原始 Phase 5 roadmap 使用概念介面：

```text
build_interpretation_contract(ranking, anchor, claims=None)
```

Phase 3.5 與 Phase 4 完成後，正式架構已更清楚，因此新版固定為：

```text
build_interpretation_contract(
    base_ranking,
    anchor,
    personalization=None,
    local_windows=None,
    locked_forecast=None,
)
```

修正目的不是擴張 Phase 5，而是保留已建立的 authority boundary：

- `base_ranking` 永遠是 Phase 3 truth。
- `personalization` 是 Phase 4 bounded metadata，不能冒充 ranking truth。
- `local_windows` 是 Phase 3 已算好的 timing interpretation metadata，Phase 5 不重算。
- `locked_forecast` 是 Phase 1 immutable prospective boundary，Phase 5 驗 digest 但不改寫。
- Phase 5 不建立自己的 maturity / specificity ranking policy。

因此 Phase 5 只負責「AI 可以怎麼解讀」，不重新決定「盤面到底是什麼」。
