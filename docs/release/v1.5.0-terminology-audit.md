# Metaphysics Lab v1.5.0｜名稱與狀態語意稽核

status: RELEASED

本紀錄針對 v1.5.0 active release surface 做名稱與狀態語意稽核。目的不是回寫歷史，而是確保目前使用者文件、workflow、runner、test、artifact 與 release gate 使用同一套名稱，並在正式發布後不再把 v1.5.0 描述成 Release Candidate。

## 正式名稱

- 核心契約：`林氏天機 v1.5｜預測與驗證契約`
- 12-case deterministic gate：`林氏天機預測驗證`
- 三檔自動檢查：`v1.5 發行面驗證`
- 正式 C.2 對話 gate：`v1.5 隔離沙盒對話驗證`
- 整體 release decision gate：`v1.5 最終發行資格驗證`
- 使用者包：`Metaphysics-Lab-v1.5.0-User-Package.zip`
- Project Instructions 發行檔：`project_instructions.txt`

## 正式發布狀態

- v1.5.0 已於 `2026-08-29` 正式發布。
- 正式 release commit：`66f604222caadac0209125a78674c3f4491c4b99`。
- 正式 Release workflow：`33246674305`。
- full repository regression：`926/926 PASS`。
- 林氏天機預測驗證：`12/12 PASS`。
- v1.5 發行面驗證：`8/8 PASS`。
- v1.5 隔離沙盒對話驗證：critical rubric `8/8 PASS`。
- User Package SHA256：`a7fe693a8bd91f6b720409e5e23e655cc3bf13396e21c8b7f99f79bc2b23cbfc`。

## 已清除的 active 舊稱／過期狀態

1. `cat-eye` / `cat_eye` / `CatEye` / `貓眼測試`。
2. 把三檔自動 gate 誤叫成 `sandbox black-box` / `three-asset black-box`。
3. v1.5 validation workflow 仍叫 `Feature Historical Calibration Validation`。
4. v1.5 artifact 名仍帶 `phase6-rebuilt-*` 或 `progressive-case-*`。
5. v1.5 已正式發布後，active current-state 文件仍把 v1.4.0 寫成最新正式版本。
6. v1.5 已正式發布後，active current-state 文件仍寫 `Release Candidate`、`PENDING_FINAL_QUALIFICATION` 或其他 pending final-release 狀態。
7. README / 快速開始 / ChatGPT Project 安裝文件未把三檔 User Package ZIP 當成首選下載面。
8. `docs/架構說明.md` 首段仍把 v1.4.0 寫成目前正式版本。
9. v1.5 active Implementation Plan / Phase 5 design 中殘留舊驗證名稱。
10. C12 progressive isolation 測試使用錯誤的 validator status 期望值；production `validate_blind_source_case` 固定回傳 `compatible`，只修測試、不修改 production validator。

## 有意保留，不視為舊稱污染

以下內容是正式技術／歷史識別，不做無差別改名：

- 已發布 v1.4.0 snapshot 內的 `project_instructions.md`。
- v1.4.0 以前的 Release Notes、舊 CI run/log、舊 commit message 與 phase-gate evidence。
- v1.5.0 正式發布前的 RED / FAIL / Release Candidate 歷史證據，例如舊 C.2 FAIL；這些保留原始狀態，不事後漂白。
- Phase 1～6 的正式 phase 名稱與既有 method/policy IDs，例如 `lin_tianji_rank_v1-exp`。
- 技術文件中的 `Historical Blind Calibration`、`Historical Activation Selector` 等既有 component / phase 名稱；一般使用者說明則優先使用白話的「歷史事件校準」。
- GitHub 自動產生的 `Source code (zip)` / `Source code (tar.gz)` 名稱。

## 自動防回歸

active status contract 應固定檢查：

- README / VERSION / CHANGELOG / 安裝與更新文件將 v1.5.0 視為目前正式版本；
- active current-state 文件不再使用 `v1.5.0 Release Candidate`、`PENDING_FINAL_QUALIFICATION`、`PENDING_FINAL_EVIDENCE` 等發版前狀態；
- README / 快速開始 / 安裝文件使用 User Package ZIP 與 `project_instructions.txt`；
- v1.4.0 Release Notes 與舊 qualification evidence 仍維持歷史 snapshot；
- capability implementation / maturity / routing 仍以 `runtime_info` 為執行權威，release 本身不自動 promotion。

## 驗證狀態

v1.5.0 正式發行 gate 已完成並發布。後續 post-release 文件 normalization 需自行通過 repository full regression，且不得改動 v1.5.0 tag、Release target、已發布 assets 或 capability maturity。
