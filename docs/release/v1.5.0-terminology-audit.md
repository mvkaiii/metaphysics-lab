# Metaphysics Lab v1.5.0｜名稱與狀態語意稽核

status: ACTIVE_TERMINOLOGY_CLEAN_PENDING_FULL_CI

本紀錄針對 v1.5.0 Release Candidate 的 active release surface 做名稱與狀態語意稽核。目的不是回寫歷史，而是確保目前使用者文件、workflow、runner、test、artifact 與 release gate 使用同一套名稱。

## 正式名稱

- 核心契約：`林氏天機 v1.5｜預測與驗證契約`
- 12-case deterministic gate：`林氏天機預測驗證`
- 三檔自動檢查：`v1.5 發行面驗證`
- 正式 C.2 對話 gate：`v1.5 隔離沙盒對話驗證`
- 整體 release decision gate：`v1.5 最終發行資格驗證`
- 使用者包：`Metaphysics-Lab-v1.5.0-User-Package.zip`
- Project Instructions 發行檔：`project_instructions.txt`

## 已清除的 active 舊稱／過期狀態

1. `cat-eye` / `cat_eye` / `CatEye` / `貓眼測試`。
2. 把三檔自動 gate 誤叫成 `sandbox black-box` / `three-asset black-box`。
3. v1.5 validation workflow 仍叫 `Feature Historical Calibration Validation`。
4. v1.5 artifact 名仍帶 `phase6-rebuilt-*` 或 `progressive-case-*`。
5. v1.5 尚未發布卻在 `VERSION.md` 寫成最新正式發布。
6. `CHANGELOG.md` 同時把 v1.5 寫成已發布、又保留 Phase 4/5 `Unreleased` 的矛盾狀態。
7. README / 快速開始 / ChatGPT Project 安裝文件未把三檔 User Package ZIP 當成首選下載面。
8. `docs/架構說明.md` 首段仍把 v1.3.0 寫成目前正式版本。
9. v1.5 active Implementation Plan / Phase 5 design 中殘留舊驗證名稱。
10. C12 progressive isolation 測試使用錯誤的 validator status 期望值；production `validate_blind_source_case` 固定回傳 `compatible`，本次只修測試，不修改 production validator。

## 有意保留，不視為舊稱污染

以下內容是正式技術／歷史識別，不做無差別改名：

- 已發布 v1.4.0 snapshot 內的 `project_instructions.md`。
- v1.4.0 以前的 Release Notes、舊 CI run/log、舊 commit message 與 phase-gate evidence。
- Phase 1～6 的正式 phase 名稱與既有 method/policy IDs，例如 `lin_tianji_rank_v1-exp`。
- 技術文件中的 `Historical Blind Calibration`、`Historical Activation Selector` 等既有 component / phase 名稱；一般使用者說明則優先使用白話的「歷史事件校準」。
- GitHub 自動產生的 `Source code (zip)` / `Source code (tar.gz)` 名稱。

## 自動防回歸

`tests/test_v15_terminology_contract.py` 固定檢查：

- active v1.5 runner / fixture / test / workflow 路徑使用新名稱；
- active release surface 不含 retired validation labels；
- README / 快速開始 / 安裝文件使用 User Package ZIP 與 `project_instructions.txt`；
- `VERSION.md` 正確區分 v1.4.0 正式版與 v1.5.0 Release Candidate；
- `CHANGELOG.md` 不再把同一 v1.5 同時寫成正式版與 Unreleased；
- 更新文件保留 v1.4 歷史 `.md` 真相，同時新增 v1.4→v1.5 `.txt` 更新流程。

## 驗證狀態

- terminology contract：5/5 PASS（one-shot normalization qualification）
- renamed prediction / release-surface suites：12/12 PASS（one-shot normalization qualification）
- exact-head full repository validation：PENDING
- v1.5 隔離沙盒對話驗證：PENDING

本檔不把前兩項局部 PASS 冒充 final release qualification；最終狀態以 release-candidate exact-head CI 與獨立 C.2 證據為準。
