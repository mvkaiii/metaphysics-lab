name: ai-dist-task0-red
on:
  pull_request:
    branches: [main]
jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.9'
      - name: Run Task 0 RED
        run: python -m unittest tests.test_ai_contract_docs -v
