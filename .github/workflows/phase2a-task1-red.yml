name: Phase 2A Task 1 RED

on:
  pull_request:
    branches:
      - feature/ziwei-transformation-flying-core-v1

jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: python -m pip install -r requirements.txt
      - name: Run Task 1 RED test
        shell: bash
        run: |
          set -o pipefail
          python -m unittest tests.test_ziwei_phase2a_models -v 2>&1 | tee task1-red.txt
      - name: Upload RED evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase2a-task1-red-evidence
          path: task1-red.txt
