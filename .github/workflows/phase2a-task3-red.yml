name: Phase 2A Task 3 RED

on:
  pull_request:
    branches:
      - feature/ziwei-transformation-flying-core-v1

jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: python -m pip install -r requirements.txt
      - name: Run Task 3 RED test
        shell: bash
        run: |
          set -o pipefail
          python -m unittest tests.test_ziwei_transformations -v 2>&1 | tee task3-red.txt
      - name: Upload RED evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase2a-task3-red-evidence
          path: task3-red.txt
