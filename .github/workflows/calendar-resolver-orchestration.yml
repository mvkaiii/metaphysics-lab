name: Calendar Resolver orchestration

on:
  pull_request:
    branches:
      - feature/calendar-resolver-v1

jobs:
  tests:
    runs-on: ubuntu-latest
    env:
      PYTHONTZPATH: ""
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install -r requirements.txt
      - name: Run Gate 0 through 3 and preserve output
        shell: bash
        run: |
          set +e
          python -m unittest tests.test_calendar_precision tests.test_calendar_models tests.test_calendar_timezone tests.test_calendar_lunar tests.test_calendar_resolver -v 2>&1 | tee test-output.txt
          status=${PIPESTATUS[0]}
          echo "$status" > test-exit-status.txt
          exit "$status"
      - name: Upload orchestration output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: calendar-orchestration-output
          path: |
            test-output.txt
            test-exit-status.txt
